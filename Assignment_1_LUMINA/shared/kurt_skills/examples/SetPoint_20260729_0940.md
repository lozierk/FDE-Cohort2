# Set Point — STE writing skill exploration (light investigation complete)

**Date:** 2026-07-29 09:40 · **Owner:** Kurt Lozier · **Machine:** MacBook Pro

State snapshot of a finished unit of work: the light investigation Kurt requested into
the "cure for AI slop" STE approach. Nothing is in progress; the next action is a
discussion, which Kurt will initiate when he has time.

## What exists

- `ideas/ste-writing.md` — **the source of truth.** Contains the findings, the sources
  (woosal1337's GitHub repo and YouTube video), the relevance-to-Kurt analysis, the
  circularity caveat, and the ~60–90 min investigation plan (read materials → baseline-lint
  Kurt's own docs → three-way head-to-head → decide).
- A `project` memory (`ste-writing-exploration`) pointing at that file.

## Decisions made

- Landing spot: `~/claude-skills/ideas/` per workshop convention (one .md per idea;
  graduates to its own folder only if adopted).
- Framing: STE is a mechanized, lintable version of Kurt's existing Hemingway mandate.
  Only STE-*flavored* mode is a candidate for Kurt's audiences; strict mode is not.
- The -74% benchmark number is treated as suggestive, not proof — the metric is STE
  violations, so the STE skill winning is partly circular. Blind human read is the real test.

## Explicitly NOT decided

Whether to adopt, adapt into `/write`, keep only the linter, or discard. That is the
whole point of the upcoming discussion.

## Resume by

Kurt says something like "let's pick up the STE / AI-slop idea." Read
`ideas/ste-writing.md`, summarize the findings back, and start at step 1 of its plan —
discussion first, execution only after Kurt agrees.

## Do not

- Do not install the STE skill or modify `/write` before the discussion.
- Do not re-investigate from scratch — the ideas file already holds the findings.
- Do not treat lint scores as the success criterion; the blind read decides.
