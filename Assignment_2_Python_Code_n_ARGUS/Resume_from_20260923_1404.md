# Resume — Assignment 2 (Session 1 close)

**Date:** 2026-09-23 14:04 ET · **Owner:** Kurt Lozier · **Supersedes:** none (first session in this folder)

**TLDR:** Session 1 read both briefs, confirmed the Cohort 1 Moment Search work is a
reference for Part 2 (not a starting point), and built, verified, ran, and committed
Part 1 end to end: sub-query division (required), RBAC-aware semantic cache (bonus),
both stretches. Commit `403fb26` on lozierk/FDE-Cohort2. Part 1 is NOT yet submitted
to Maven. Part 2 (Moment RAG on a YouTube transcript) is not started; recon is done.
**Next: Kurt picks the submission form for Part 1 and the video for Part 2; then
build Part 2.**

---

## Quick re-start prompt (copy-paste)

> Resuming Assignment 2 (Hamza FDE Cohort 2) — Part 1 done, Part 2 Moment RAG next.
>
> Please read `Assignment_2_Python_Code_n_ARGUS/CLAUDE.md` and
> `Assignment_2_Python_Code_n_ARGUS/Resume_from_20260923_1404.md` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next
> work step.

## Detailed starter prompt (fallback)

**Context.** Two-part assignment due Fri 2026-09-25, parts submitted separately.
Part 1 extends the course's Agentic Router notebook (OpenAI `gpt-5.6-luna`, nomic
embeddings, prebuilt Qdrant index of OpenAI docs + Lyft/Uber 10-Ks, SerpApi web
route). Both stubs are implemented and the notebook ran green twice (36 code cells,
zero errors). Part 2 asks for a baseline chunk-RAG vs a Moment-RAG-style pipeline on
one YouTube transcript, compared, with a deck or Loom as deliverable.

**Working style:** orchestrator rule (delegate reads and routine builds to
sonnet-builder/haiku-mechanic; keep design and review inline); confirm understanding
before multi-step work; never read `.env`; don't fabricate numbers; momentum over
ceremony near the deadline.

**Files to read first, in order:**
1. `CLAUDE.md` (this folder) — layout and rules.
2. This file — queue and gates.
3. `docs/recon/recon_momentsearch.md` — what a "moment" is in the reference codebase
   (it's a 15 s query-time fusion window, not a stored object; no LLM in ingestion).
4. `docs/recon/recon_muthukumar.md` — the classmate bar to beat and his gaps.
5. Only if touching Part 1: `part1_agentic_router/README.md`.

**Where we paused.** Kurt asked for the commit and the close right after reviewing
the Part 1 run summary. He has not yet said whether Part 1 goes to Maven as an .ipynb
upload or also as a public repo, and has not chosen the Part 2 video.

**Immediate goals, ordered, with gates:**
1. **Submit Part 1.** GATE: Kurt's choice of form. If public repo: snapshot only
   `part1_agentic_router/` (notebook, README, harness, .env.example, .gitignore) plus
   `docs/recon/`; the index and venv are gitignored. Post text: what was built, the
   three check-yourself cases, the leak test, the offline harness.
2. **Choose the Part 2 video.** GATE: Kurt. Not the Jobs talk. Options: a Cohort 1
   talk already transcribed (e.g. Karpathy) or something new. Needs English captions
   and clear topic structure; 20–60 min is the sweet spot.
3. **Build Part 2** in `part2_moment_rag/` (small Python scripts, no deploy):
   transcript fetch → baseline (fixed chunks, local embeddings, Chroma or Qdrant,
   Claude answers) → moment pipeline (semantic boundaries via embedding drift + one
   LLM pass to label/bound moments, moment summaries embedded, multi-moment retrieval,
   Claude answers with timestamps) → golden set of 10+ questions → scorecard (recall@k,
   citation accuracy, tokens, latency) → deck. Reuse the Cohort 1 eval pattern
   (`Assignment_3_Moment_Search/eval/golden_set.json` + `scripts/run_eval.py` shape).
4. **Deck** (Option 1, like A1): video + why, codebase understanding (reuse Cohort 1
   briefs + the fusion-window finding), baseline, moment build, comparison, findings.

**Open questions for Kurt:** (a) Part 1 submission form? (b) Part 2 video?
(c) Anthropic key available for Part 2 answers (planned: Haiku for cheap passes,
Sonnet 5 for answers, per the A1 model split)?

**Newly-dropped unmined files:** none.

**Start by:** confirming files read → 3–5 sentence state summary → ask (a) and (b)
→ offer: draft the Part 1 submission post now (delegable) vs. start the Part 2
transcript + baseline scaffold while Kurt decides the video.

## Session log

**Created:** `docs/recon/` (three recon summaries + original notebook),
`part1_agentic_router/` (executed notebook, README, offline_harness.py,
smoke_qdrant.py, .env.example, .gitignore), `CLAUDE.md`, this file.
**Memory:** `a2-state` (project) saved and indexed.

**Decisions made:** keep OpenAI in Part 1 to match the grader; tagged-by-source cache
design (shares OpenAI-docs rows across roles, no flush on role change); leave the
router-before-permission order as the notebook specifies; internet citations resolved
to title + URL in the sources footer; Part 2 differentiators as listed above.
**Not decided:** Part 1 submission form; Part 2 video; Part 2 vector store.

**Mid-session direction from Kurt:** submit parts separately as each completes; Part 1
fast, more time on Part 2. Corrected me: A1 used Tavily, SerpApi was only an option
(he then obtained a SerpApi key for this notebook).

**Commits (lozierk/FDE-Cohort2):** `403fb26` A2 Part 1 complete; close commit follows.

## Session closed — 2026-09-23 14:04 ET

**State:** Part 1 built, run green, committed, unsubmitted. Part 2 recon complete,
build not started. Keys in `part1_agentic_router/.env` (OPENAI_API_KEY, SERP_API_KEY).

**Resume by:** pasting the quick re-start prompt above into a fresh session in this
folder.

**Do not:**
- Do NOT read, cat, or ls any `.env`; key names and lengths only.
- Do NOT edit the notebook's original cells (graded scaffolding); only the stub cells
  and the cells added after them are ours.
- Do NOT re-execute the notebook casually; each full run costs ~20 OpenAI calls and
  ~5 SerpApi searches (free tier is 100/month). Use `offline_harness.py` for logic.
- Do NOT commit `Agentic_RAG/` (14 MB index), `.venv/`, or `.env`.
- Do NOT reuse the classmate's video or rule-based moment approach.
- Do NOT quote latency or speed-up numbers beyond what the executed notebook shows
  (concurrent vs sequential ~1.3x; the web call dominates).
- Do NOT push to any remote other than lozierk/FDE-Cohort2.
