# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-14 15:27 ET · **Session:** 5 (opened 14:05, closed 15:27 ET, 2026-09-14) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260914_1320.md`

**TLDR:** Week 2 part A is built, reviewed, measured on Atlas with real embeddings, and committed:
Spaces, the child-process jobs worker, page-aware parsing, hybrid RAG with RRF, `search_documents`,
and doc citations with locators. Recall@5 is 39/39 on the gold set, 202 accept is 181–212 ms,
docs-mode TTFT p95 is 1.62 s (was 12.35 s until Kurt approved answering straight from the
preflight), $0.002 per docs answer. Part B (deep search) has a written spec and is the next build.
Still queued from before: the Sonnet-for-synthesis A/B and Kurt's web-mode TTFT call. Then
`runs/failing/`, bench, deploy (agent on Fly because the worker is a child process), eval.
**Deadline Fri 2026-09-18, three and a half days.**

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260914_1527.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## Detailed starter prompt (fallback)

**Context.** Kurt is in Hamza Farooq's FDE bootcamp, cohort 02. Assignment 1 is LUMINA, a
Perplexity-style search agent. Claude builds solo in `Claude_Build/`; Codex is paused. The course
provides the UI, zod contract, benchmark, grader, gold set, and quality gate; we build only
`backend/agent/` and `backend/gateway/`. Editing a provided folder (`web/ packages/contract/
benchmark/ eval/ quality/ scripts/`) is an automatic fail. The submission is one Vercel URL whose
`/evals` page renders a real eval report against the deployed app. Deadline Fri 2026-09-18.

**Working-style reminders.** Confirm understanding before multi-step work. Review Kurt's thinking
on consequential decisions (he decides model and loop design; you recommend with numbers, and he
approves fast when the numbers are there). One source at a time with checkpoints. Don't fabricate;
say "I don't know". Converge, don't repeat. Orchestrate per CLAUDE.shared.md: delegate a written
spec to opus-specialist, run a read-only opus-specialist review in parallel with your own review,
patch inline, measure yourself. Kurt is never the relay between agents. Kurt distrusts compaction:
recommend `/close resume` at the next natural checkpoint around 25–35% context.

**Files to read first, in order:**
1. `MESSAGE_BOARD.md` then `board/STATE.md` (assignment root) — entry point and current state.
2. `board/read.sh claude list` then `print` — unread messages; ack with the printed token.
3. `Claude_Build/docs/week2-deep-build-spec.md` — part B, the next build. Written against the
   bench's phase 4 checks; read §1 (what is graded) and §4 (the loop) before launching.
4. `Claude_Build/docs/week2-rag-build-spec.md` — part A as built, with §1 explaining the recall
   and grounding rules the registry now satisfies.
5. `Claude_Build/backend/agent/src/loop/quick.ts` — the loop as it runs today: web preflight,
   docs/auto preflight, `skipResearch` for docs mode, `DOCS_EXTRA_SEARCHES` budget. Part B
   extracts phase 1 of this file into `loop/research.ts`.
6. `Claude_Build/backend/agent/src/loop/sources.ts` — registry: doc sources keyed by
   `docId#locator`, `mergePieces`, `passageTitle`. Part B adds `subQuestion`.
7. `Claude_Build/backend/agent/src/tools/search-documents.ts`, `retrieval/search-chunks.ts`,
   `ingest/*.ts`, `worker.ts`, `routes/spaces.ts` — only if part B or a defect touches them.
8. `Claude_Build/fixtures/README.md` — the pinned 60-page PDF used in both specs' acceptance.
9. `Claude_Build/DESIGN.md` v1.0 and `docs/week1-build-spec.md` — background only.

**Where we paused.** Working tree clean at `6a900a3`. Local services stopped. `.env` complete
(Anthropic workspace-scoped key, Tavily, OpenAI with credit as of today, Atlas URI) plus an
appended `RATE_LIMIT_PER_MINUTE=300` for the bench. Atlas `lumina-claude` holds the bench Space
`spc_mu1m6az2139fzn` (user `kurt-test`: the four gold corpus files plus `bench-60p.pdf`, all
`indexed`) and two leftover test Spaces from the builder (`spc_mu1l7uvtwd49s4` with four `failed`
rows from the no-credit run, `spc_mu1l9wqh2k5tda` with 41 fake-embedder chunks). Deleting them was
blocked by the permission classifier; they are Space-filtered and harmless until Kurt decides.

**How to run locally (from `Claude_Build/`):**
```
(cd backend/agent  && LOG_LEVEL=debug npx tsx src/index.ts &)   # agent :8000 + forked worker; reads ../../.env
(cd backend/gateway && npx tsx src/index.ts &)                   # gateway :8787
curl -s localhost:8787/health
python3 backend/agent/bin/upload.py eval/gold/corpus/*.pdf eval/gold/corpus/*.md   # new Space, 4 uploads, waits for indexed
python3 backend/agent/bin/recall.py <spaceId>          # all 39 gold questions, recall@5 scored the bench's way; --id g07 for one
python3 backend/agent/bin/ask.py "question" /tmp/q.json # one web ask: tools, TTFT, cost, cites
```
Tests: `npm test` in `backend/agent` (64) and `backend/gateway` (9); `npm run typecheck`.

**Measured today (Atlas, real providers, 39 gold questions, mode docs):**

| Metric | Gate | Before docs-TTFT change | After (committed) |
|---|---|---|---|
| Recall@5 | ≥ 0.70 | 39/39 | 39/39 |
| TTFT p95 | ≤ 2.5 s | 12.35 s | 1.62 s |
| Mean cost | ≤ $0.05 | $0.0095 | $0.0020 |
| 202 accept (4 files + 60p) | p95 ≤ 300 ms | 181–212 ms | same |
| Web ask during 60-page ingest vs idle | ≤ 1.3× | 7.7 s vs 6.8 s (≈1.1×, ingest lasted 4 s) | same |

Web-mode idle TTFT today: 4.8–6.2 s on fresh questions (unchanged; the web TTFT change is still
Kurt's open call, see goal 3).

**Immediate goals for next session, in order, with gates:**

1. **Part B: deep search (gate: none).** Launch opus-specialist on
   `docs/week2-deep-build-spec.md` with the same prompt shape as part A (read order, absolute
   rules, run typecheck + tests, one real deep ask, report per §10). In parallel, a read-only
   opus-specialist review of the diff. Then acceptance §8 steps 1–5 yourself: read three real
   plans out loud, check ratio ≥ 2× on the four `benchmark/queries.json` deep questions, cost
   ≤ $0.35, cap probe → 429 with `resetsAt`, deep-under-ingest with the fixture. Budget ≈ $1.
2. **Sonnet 5 for synthesis A/B (gate: none; Kurt approved exploring).** As in the previous
   resume: `LLM_MODEL_SYNTHESIS`, `providers.synthesisLlm`, per-model rates in `config/model.ts`,
   `/health` names both when they differ. Run the five web questions plus five gold docs
   questions on both; decide with Kurt. ≈ $0.30.
3. **Web-mode TTFT — Kurt's call, present once.** Docs mode now has the precedent: the loop's
   own preflight answered, synthesis started, TTFT 1.6 s. The web equivalent: preflight search
   without raw content, fetch top 3 in parallel with a 3 s timeout, straight to synthesis when
   ≥ 2 have text, else the research turns. Expected ≈ 3 s cold. Alternative: document the miss.
4. **Regenerate `runs/failing/`** with a real provider failure (invalid Tavily key), per AGENTS.md.
5. **Bench, deploy, eval (gate: 1, and Q-4 ceiling from Kurt).** `npm run bench` locally against
   `localhost:8787` with `RATE_LIMIT_PER_MINUTE=300`; estimate spend from measured costs before
   the full run. Deploy: the agent must run on a long-lived host because the worker is a forked
   child process — Fly for the agent (private) as DESIGN.md says; the gateway on Fly or Vercel.
   Set the same rate limit in the deployed gateway's env. Then the eval skill against the
   deployed gateway; `/evals` renders design answers, run notes, and two trajectories.
6. **DESIGN.md updates** where the build diverged: docs-mode answers from the preflight; doc
   sources merged per page; no re-rank step and why; worker probe waits ≈ 3 s for Atlas Search;
   rate limit for the bench; Vercel ruled out for the agent.
7. **After eval, if time remains:** OpenRouter adapter and the Haiku vs GLM-5.3-flash vs Kimi
   K2.6 A/B as an `/evals` section (prices in the previous resume).

**Lessons from this session worth carrying:**
- Design against the grader's arithmetic, not the prose: the recall hit rule (top-5 doc sources,
  page match or anchor-in-snippet) and the grounding haystack (keyed by docId+locator, top-5
  only) dictated chunk-per-page, whole-chunk snippets, and the same-page merge.
- A model turn is ≈ 2 s on Haiku. When the loop already knows the answer to "what should I do
  next", do it in code and say so in the trace `reason`. Every recall hit came from the
  preflight; the model's extra searches only cost time.
- Atlas Search lags writes by ≈ 3 s; the probe is what earns `indexed`.
- The first GridFS upload of a process paid 456 ms for driver index checks; ensure at boot and
  keep one bucket per process.
- The gateway's default 30/min rate limit would have failed the bench's own polling.

**Open questions, by person:**
- *Kurt:* delete the two leftover Atlas Spaces (approve the cleanup or run it); web-mode TTFT
  change (goal 3); whether Sonnet stays for synthesis after the A/B; Q-4 spend ceiling before
  the bench; Q-5 which URL is submitted; gateway deploy target (Fly vs Vercel); Codex's scope.
- *Codex:* nothing requested.
- *Claude:* none blocking.

**Newly dropped files, not yet read by Claude — do not assume their content:** none.

**Start by:** confirm the files above were read → 3–5 sentence state summary → ask the gating
question ("Launch part B deep search now from the spec, or run the Sonnet A/B first?") → offer
two options: (a) part B now (≈ 2–3 hours incl. review and acceptance, ≈ $1; it is 15 points and
gates the bench); (b) Sonnet A/B first (≈ 1 hour, ≈ $0.30) so part B's synthesis prompt is
written for the model that stays.

---

## Session log

**Files created/modified (all under `Assignment_1_LUMINA/Claude_Build/`):**
- `docs/week2-rag-build-spec.md` (new), `docs/week2-deep-build-spec.md` (new), `fixtures/`
  (`bench-60p.pdf`, `make-bench-pdf.mjs`, `README.md` with sha256) — commit `52e4705`.
- Part A build (commit `b941f39`): new `src/config/rag.ts`, `src/store/gridfs.ts`,
  `src/routes/spaces.ts`, `src/ingest/{parse,chunk,probe,index-document}.ts`,
  `src/retrieval/search-chunks.ts`, `src/tools/search-documents.ts`, `bin/upload.py`,
  `bin/recall.py`, `test/{ingest,spaces,retrieval,worker}.test.ts`; edited `env.ts`, `db.ts`,
  `index.ts` (fork worker, graceful shutdown), `worker.ts`, `store/index.ts`, `loop/{quick,
  sources,prompts}.ts`, `routes/ask.ts`, `tools/{index,types,memory}.ts`, `.env.example`.
  Review fixes applied inline: child inherits `VECTOR_BACKEND`; restart backoff grows; one
  GridFS bucket per process; graceful SIGTERM; PDF `pages` = real count; `userId` filter in both
  retrieval pipelines.
- Docs-mode TTFT (commit `3d82e0a`): `quick.ts` `skipResearch` + `DOCS_EXTRA_SEARCHES` budget,
  `config/rag.ts`, `env.ts`, `.env.example`, `test/retrieval.test.ts`.
- Rate limit (commit `6a900a3`): `.env.example`; `.env` appended (never committed).
- Board: `CLAUDE-068` (this close); `board/STATE.md` refreshed. Memory: `lumina-week1-state`
  updated, `lumina-env-and-atlas-guardrails` new. Brain: `projects/fde-cohort2-lumina.md`.

**Decisions made:** Week 2 build before the Sonnet A/B (Kurt); same-page chunks merge into one
doc source (Kurt approved Claude's recommendation); docs mode answers straight from the preflight
when it found anything, with one extra model-requested document search allowed, enforced in the
loop (Kurt approved); no re-rank step, reason documented in `retrieval/search-chunks.ts` (Claude);
chunks never cross a PDF page or Markdown section, snippet = whole merged chunk text (Claude, from
the bench's rules); worker stays a forked child process, which rules Vercel out for the agent
(consequence of Kurt's earlier decision); `RATE_LIMIT_PER_MINUTE=300` for bench and deploy, code
default stays 30 (Kurt); 60-page bench PDF pinned as a fixture with hash (Kurt).

**Explicitly NOT decided:** web-mode TTFT change; Sonnet for synthesis; Q-4 spend ceiling; Q-5
URL; gateway deploy target; deletion of the two leftover Atlas Spaces; Codex scope.

**Mid-session direction from Kurt:** "Week 2 first, write the spec and show me"; "Proceed and
launch"; store the 60-page PDF in the working folder as a consistent test standard and for the
write-up; add the fixture to part B's acceptance; commit the fixture and specs; OpenAI topped up
("refill had been paused"); approve the same-page merge; approve the docs-mode change; raise the
rate limit via env; then `/close resume`.

**Spend this session (approx.):** Anthropic ≈ $0.60 (≈ 90 docs asks across three recall runs and
probes, plus 6 web asks); OpenAI embeddings < $0.01; Tavily ≈ 8 credits; Atlas M0 free. Delegated
agents: one opus-specialist build (≈ 25 min), one opus-specialist review (≈ 7 min).

**Commits (`lozierk/FDE-Cohort2`, `main`):** `52e4705` specs + fixture · `b941f39` part A ·
`3d82e0a` docs-mode TTFT · `6a900a3` rate limit doc · close commit follows this file.

---

## Session closed — 2026-09-14 15:27 ET

**State:** Week 2 part A live on Atlas and measured; part B spec ready; Sonnet A/B and web TTFT
still queued; no deploy yet. Three and a half days to the deadline.

**Resume by:** pasting the Quick re-start prompt into a new Claude Code session opened in
`/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/`.

**Do not:**
- Do not re-open Haiku, the same-page merge, or the docs-mode preflight path; Sonnet is for the
  synthesis call only unless Kurt says otherwise; OpenRouter waits until after eval.
- Do not read `.env` by any means (deny rule; `test -f` only; append-only writes are allowed).
- Do not run multi-collection deletes on Atlas without Kurt's explicit go; the classifier blocks
  them anyway.
- Do not edit the starter's provided folders, ever; do not relax any threshold or cap; do not
  raise `MAX_TOOL_CALLS`, the wall clocks, or `DEEP_DAILY_CAP` to pass a gate. The rate limit is
  ours, not a gate, and is already set.
- Do not put `plan_research` into `ALL_TOOLS` or `toolsForMode()`; the deep loop calls the planner directly.
- Do not charge a deep credit when planning fails before retrieval; do not emit `plan` after any retrieval step.
- Do not build the worker on the request thread; do not deploy the agent to Vercel while the
  worker is a child process.
- Do not equate bench TTFT with live TTFT; do not pad prompts to reach a cache minimum.
- Do not edit `board/STATE.md` without claiming the lease; never edit a message file or the archive.
- Do not contact Kurt by Telegram unless something is blocking and jointly agreed.
- Do not guess timestamps; use `TZ=America/New_York date`.
- Do not let the session drift toward auto-compaction; recommend `/close resume` at the next
  checkpoint around 25–35% context.
