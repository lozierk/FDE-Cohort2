# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-15 11:26 ET · **Session:** 11 (opened ≈ 10:15 ET, 2026-09-15) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1 → Opus 4.8 this session, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260915_1009.md`

**TLDR:** **The submission is complete, live, and SUBMITTED to the Maven course home.** On
2026-09-15 Kurt posted LUMINA to the "Build a Perplexity Clone" project channel
(maven.com/boring-bot/ai-system-design); the exact text is `Claude_Build/docs/SUBMISSION.md`.
Live at https://lumina-claude.vercel.app/evals: all six gates pass, 16/16 SLA, automated 85/85,
15 manual pending. Public snapshot https://github.com/lozierk/Claude_Build_Submission at
`953fbcf`. This session also: refreshed the private Notion page from `docs/WRITEUP.md`; wrote an
office-hours brief for Hamza; reviewed three classmate deployed builds grader's-eye and ranked
all four; verified two of our own claims live; posted board CLAUDE-074 with a Codex-restart
discoveries brief; and cleaned up the `/evals` header (the `runNotes` essay was rendering as a
run-on paragraph). **Kurt's noon-2026-09-15 target (office-hours review) was met.** Nothing is
blocked. Open, all Kurt's: video by Friday, possible Codex restart after Friday, and the Friday
re-run of the eval. Deadline Fri 2026-09-18.

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260915_1126.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## What this session did (session 11), in order

1. **Recorded Kurt's noon target** (fully complete by noon ET 2026-09-15 for Hamza's office-hours
   review; Friday is the hard stop only). Memory + resume.
2. **Notion refreshed** from `docs/WRITEUP.md` (page `3dc4f6430102817ca7e1ffec97a03650`): replaced
   the provisional local-bench tables and placeholder links with the deployed run-2 tables, a
   green "submitted/live" status callout, filled submission links. Verified rendered clean.
3. **Office-hours brief** for Hamza: `docs/reviews/2026-09-15_office_hours_brief.md` (PRIVATE),
   ~640 words, four questions (provided-file call, two eval reports, snapshot repo, peer review
   as practice).
4. **Three classmate deployed reviews** (grader's eye, PRIVATE, `docs/reviews/`): Michal Olzak
   (`_michal_deployed_review.md`), Muthukumar Selvarasu (`_muthukumar_deployed_review.md`), plus
   the earlier Saurabh. **Four-way ranking** `_four_way_ranking.md`: **K > MO > SB > MS**; the
   MO-vs-SB order flips on whether the grader re-runs `eval.mjs`.
5. **Two of our claims verified:** (a) a verbatim repeat sets `done.searchCached: true` live (cold
   false/1100 ms → repeat true/5 ms; `req_84a49150-3c8`, `req_bca41c06-b0b`); (b) ingest never
   marks a doc `indexed` before the read-your-write probe (`index-document.ts`; tests
   `ingest.test.ts:191` pass, `:298` loud-fail). Both recorded in the ranking's carry-backs.
6. **Submitted to Maven** (Kurt, via the browser box I filled; he bolded four items and posted).
   `docs/SUBMISSION.md` = the exact text.
7. **Board CLAUDE-074** + `shared/CLAUDE_BUILD_DISCOVERIES_FOR_CODEX.md`: the discoveries a
   restarted Codex build should start from. STATE.md refreshed under a claimed/released lease.
8. **`/evals` header cleanup.** The provided UI (`web/src/EvalsPage.tsx`, lines 71–78) renders
   `report.runNotes` inline in the header sub-line; our `docs/run-notes.txt` was a 2,470-char
   two-run essay, so it showed as a run-on paragraph. Shortened to a 602-char `·`-separated
   overview (Kurt's call to KEEP a substantive intro, not trim to a bare label — a "sales"
   framing; see the new style memory). Rebuilt `reports/latest.json` locally (notes + deployedAt
   only; 85/85 unchanged), Kurt redeployed the gateway, verified live, rebuilt the public snapshot
   and Kurt pushed (`b5306b8..953fbcf`).

---

## Where we paused

Working tree clean at `f4aa723` (4 session-11 commits on `main`, **not pushed** to the private
`FDE-Cohort2`). Live: agent release v4 unchanged; **gateway redeployed this session** (serves the
602-char report); Vercel unchanged. Public snapshot at `953fbcf` (205 files) — pushed.

**The public snapshot must be built on a CLONE of the live repo, not an empty dir**, or the push
is rejected for divergent history (learned this session). Re-clone with
`git clone https://github.com/lozierk/Claude_Build_Submission.git <OUT>`, then
`OUT=<OUT> scripts-local/assemble-public.sh "<msg>"`, then Kurt pushes. This session's clone is at
the scratchpad `.../pub2` (gone when the session ends).

**Live check (30 s):**
```
curl -s https://lumina-claude-gateway.fly.dev/health
curl -s https://lumina-claude-gateway.fly.dev/evals/report.json | python3 -c "import sys,json;r=json.load(sys.stdin);print(r['rubric']['awarded'],[g['status'] for g in r['gates']],len(r['runNotes']),'note chars')"   # 85 [pass ×5, manual] 602
open https://lumina-claude.vercel.app/evals
```

**Measured, run 2 (served, unchanged):** TTFT p95 1185 ms · answer p95 4.9 s · deep plan p95
3619 ms · deep answer p95 73.3 s · grounding 0.985 · recall@5 30/30 · cache 100% · 0 errors ·
$0.0033 quick / $0.131 deep · quality 0 errors, 2 warnings (A3, P2).

---

## Immediate goals for next session, in order, with gates

1. **Friday re-run (gate: Kurt / the deadline).** The morning of **Fri 2026-09-18**, re-run the
   grader's path `eval/eval.mjs --deploy-url https://lumina-claude-gateway.fly.dev`, then
   `scripts-local/finish-report.sh` → Kurt deploys the gateway (`!` line) → rebuild the snapshot
   (clone!) → push. A stored 85/85 can go stale; Michal's did. One full eval ≈ $1.30.
2. **Video (gate: Kurt).** If recorded: `VIDEO=<url> scripts-local/finish-report.sh` → same deploy
   → snapshot → push → update `docs/SUBMISSION.md` and Notion.
3. **Codex restart (gate: Kurt, optional).** If Kurt un-pauses Codex after Friday, start it from
   `shared/CLAUDE_BUILD_DISCOVERIES_FOR_CODEX.md` (board CLAUDE-074).
4. **After grading:** `fly scale count 0 -a <app>` on both Fly apps if cost matters; OpenRouter
   A/B stays deferred.

---

## Open questions, by person

- *Kurt:* video by Friday; possible Codex restart after Friday; the Friday eval re-run; whether to
  pass classmate feedback (deferred to office hours); scale-to-zero after grading.
- *Codex:* nothing requested (discoveries brief posted for a possible restart).
- *Claude:* none.

**Newly written files this session, not from before:** `docs/SUBMISSION.md`,
`docs/reviews/2026-09-15_{office_hours_brief,michal_deployed_review,muthukumar_deployed_review,four_way_ranking}.md`,
`shared/CLAUDE_BUILD_DISCOVERIES_FOR_CODEX.md`, `board/messages/CLAUDE-074.md`. Shortened
`docs/run-notes.txt`. New memories: noon target, Friday re-run, post-submission intentions,
Kurt's overview-at-top style.

---

## Session log

**Commits (`lozierk/FDE-Cohort2`, `main`, not pushed):**
- `76fd4f5` three classmate reviews + four-way ranking + office-hours brief; the two verifications.
- `bde05fa` submitted to Maven; board CLAUDE-074 + Codex discoveries; STATE refreshed.
- `ccc1ba5` shorten `runNotes` to a scannable overview (2470 → 602 chars).
- `f4aa723` remove the redundant run-notes backup.
Public (`lozierk/Claude_Build_Submission`): `953fbcf` (pushed).

**Decisions made (Kurt):** keep a substantive `/evals` overview rather than a bare label (a
"sales/introduction" framing — now a saved style preference); submitted the wording as drafted
with four items bolded; classmate feedback deferred to office hours; video by Friday and a Codex
restart both optional and uncommitted.

**Live verifications (Claude, Kurt informed):** `searchCached` on a verbatim repeat; `indexed`
only after the read-your-write probe.

**Spend this session:** ≈ $0.03 (the live cache-proof asks) + review asks earlier; total ledger
still ≈ $11.30 of the $15 ceiling. No eval re-run this session (the header fix was data-only).

---

## Do not

- Do not re-run the eval unless code or config changed; run 2 is served and explained. The Friday
  re-run is the one planned exception.
- Do not edit the starter's provided folders, including `benchmark/sla.json` and
  `quality/rules.json` (Kurt's decision); do not relax any threshold or cap.
- Do not push `docs/reviews/`, `docs/peer-review/`, `Resume_from_*`, `SetPoint_*`, or the board to
  the public repo; rebuild it only with `assemble-public.sh` on a fresh CLONE, never by hand.
- Do not flip `lozierk/FDE-Cohort2` public; D-12 stands.
- Do not run Fly deploys or pushes to existing remotes yourself; hand Kurt a `!` line.
- Do not read `.env` by any means; do not run multi-collection Atlas deletes yourself.
- Do not commit `runs/`, `runs.local-*/`, `reports/`, `deploy/web/`, `.vercel/`, `.playwright-mcp/`.
- Do not exceed the $15 Q-4 ceiling without Kurt (≈ $11.30 spent).
- Do not put `plan_research` into `ALL_TOOLS` or `toolsForMode()`; do not raise the deep caps.
- Do not edit `board/STATE.md` without claiming the lease; keep it under 8 KB; never edit a message
  file or the archive.
- Do not guess timestamps; use `TZ=America/New_York date`.
