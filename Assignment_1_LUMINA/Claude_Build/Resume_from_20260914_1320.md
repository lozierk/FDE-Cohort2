# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-14 13:20 ET · **Session:** 4 (opened and closed 2026-09-14) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260914_0826.md`

**TLDR:** All keys verified, Atlas is live with every index queryable, and the loop has run for
real: five varied questions, all grounded, $0.013–0.031 each, zero dangling citations. Four
real defects found and fixed (date anchoring, PDF-as-HTML, unranked passages, over-searching).
Two findings recorded, not fixed: Haiku's 4,096-token cache minimum means our ~1,100-token prefix
never caches (do not pad), and TTFT is 3.7–7.7 s against a 2.5 s p95 target because of sequential
model turns, not search. A peer submission was reviewed in depth (lessons below). **Kurt's
direction: stay on Haiku, run a Sonnet-5-for-synthesis A/B first, defer OpenRouter/GLM/Kimi to
after eval. Next: the A/B, then Kurt's TTFT decision, then Week 2 (spaces/worker/RAG → deep →
deploy → eval). Deadline Fri 2026-09-18, four days.**

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260914_1320.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## Detailed starter prompt (fallback)

**Context.** Kurt is in Hamza Farooq's FDE bootcamp, cohort 02. Assignment 1 is LUMINA, a
Perplexity-style search agent. Claude builds solo in `Claude_Build/`; Codex is paused. The course
provides the UI, zod contract, benchmark, grader, gold set, and quality gate; we build only
`backend/agent/` and `backend/gateway/`. Editing a provided folder (`web/ packages/contract/
benchmark/ eval/ quality/ scripts/`) is an automatic fail. The submission is one Vercel URL whose
`/evals` page renders a real eval report against the deployed app. Deadline Fri 2026-09-18.

**Working-style reminders.** Confirm understanding before multi-step work. Review Kurt's thinking
on consequential decisions (he decides model and loop design; you recommend with numbers). One
source at a time with checkpoints. Don't fabricate; say "I don't know". Converge, don't repeat.
Orchestrate per CLAUDE.shared.md: delegate well-specified builds (sonnet-builder / opus-specialist)
from a written spec, review and patch inline. Kurt is never the relay between agents. Kurt
distrusts compaction: recommend `/close resume` at the next natural checkpoint around 25–35%
context, before it is needed.

**Files to read first, in order:**
1. `MESSAGE_BOARD.md` then `board/STATE.md` (assignment root) — entry point and current state.
2. `board/read.sh claude list` then `print` — unread messages; ack with the printed token.
3. `Claude_Build/backend/agent/src/loop/quick.ts` and `loop/prompts.ts` — the loop as it runs
   today, including this session's date line, quick budget, and per-call usage debug log.
4. `Claude_Build/backend/agent/src/loop/sources.ts` (`toPassages`, `PASSAGE_LIMIT`) and
   `tools/fetch-page.ts` (HTML-only guard) — this session's other two fixes.
5. `Claude_Build/backend/agent/src/providers/index.ts` and `env.ts` — where a second model for
   synthesis would be wired (step 1 below).
6. `Claude_Build/docs/week1-build-spec.md` — what exists and why. `Claude_Build/DESIGN.md` v1.0.
7. `Claude_Build/TECHNICAL.md` Part 1 items 6–8 and `SPEC.md` §5.4–5.5 — what remains to build.
8. `Claude_Build/docs/peer-review/lumina-web-sage_report_2026-09-11.json` — only if a peer
   comparison is needed; the lessons are summarised in this file.

**Where we paused.** Working tree committed at close (see Session log). `Claude_Build/.env` is
complete: Anthropic (workspace-scoped key), Tavily, OpenAI, and `MONGODB_URI` for Atlas cluster
`lumina-claude` (M0, AWS us-east-1, user `lumia`, db `lumina_claude`). `node
scripts/create-indexes.mjs --status` shows 15 regular/TTL indexes and 3 search indexes READY
(queryable). `/health` reports `claude-haiku-4-5 · tavily · atlas-vector-search · db ok`. Local
services are stopped. The ask harness is `Claude_Build/backend/agent/bin/ask.py` (start agent and
gateway first, see "How to run" below). No deploy exists yet. Codex silent since 2026-09-09.

**How to run locally (from `Claude_Build/`):**
```
(cd backend/agent  && LOG_LEVEL=debug npx tsx src/index.ts &)   # agent :8000, reads ../../.env
(cd backend/gateway && npx tsx src/index.ts &)                   # gateway :8787
curl -s localhost:8787/health
cd backend/agent && python3 bin/ask.py "question" /tmp/q.json    # prints tools, TTFT, cost, cites
grep '"llm usage"' <agent log>                                    # per-call input/output/cacheRead/cacheWrite
```
Tests: `npm test` in `backend/agent` (30) and `backend/gateway` (9); `npm run typecheck`.

**Measured baseline on Haiku 4.5 (2026-09-14, five questions, mode web, fresh threads):**

| Question | Tools | TTFT | Cost | Cites |
|---|---|---|---|---|
| Fed latest FOMC decision | 3 | 6.0 s | $0.020 | 4 ok |
| Population of Tokyo | 3 | 6.9 s | $0.019 | 1 ok |
| Prompt caching vs RAG | 3 | 6.2 s | $0.019 | 2 ok |
| OpenAI CEO | 1 | 3.7 s | $0.013 | 2 ok |
| Semaglutide side effects | 3 | 7.7 s | $0.031 | 5 ok |

Where TTFT goes (Tokyo): tool calls total < 2 s; three sequential Haiku turns ≈ 1.6 s each
before synthesis starts. Tavily with `include_raw_content:true` measured 3–11.5 s and returned
1–4 of 5 results; without it 1.3–2.3 s and all 5. Search cache hit returns in ~130 ms.

**Immediate goals for next session, in order, with gates:**

1. **Sonnet 5 for synthesis A/B (gate: none; Kurt approved exploring it).** Add an optional
   second model for the synthesis call only: `LLM_MODEL_SYNTHESIS` in `env.ts` (default = the
   main model), a second `AnthropicLlm` in `providers/index.ts` (`providers.synthesisLlm`), and
   use it in `quick.ts` phase 2. Extend `config/model.ts` so `costUsd` prices each call at its own
   model's rates (Sonnet 5: $2 in / $10 out / $0.20 cache read / $2.50 cache write per MTok).
   `/health` must still name the model honestly — report both if they differ. Then run the same
   five questions above with `LLM_MODEL_SYNTHESIS=claude-sonnet-5` and compare answers side by
   side with the Haiku transcripts (saved outputs in `bin/ask.py` JSON). Budget ≈ $0.25. Decide
   with Kurt on evidence: answer quality, TTFT delta, cost delta, and whether Sonnet's 1,024-token
   cache minimum now yields `cacheRead > 0` on the research prefix if the main model is also
   switched (that is a separate, larger question; do not switch the research model without Kurt).
2. **TTFT design decision — Kurt's call, present it once with the numbers above.** Recommended
   change to the fresh-thread web path in `quick.ts`: preflight search *without* raw content,
   fetch the top 3 results in parallel with a ~3 s timeout, and go straight to synthesis when
   ≥ 2 candidates have page text; otherwise fall into the existing research turns. Expected ≈ 3 s
   cold, ≈ 1.5 s on a cached search. The bench workload is 50% repeats and 30 doc queries, so the
   p95 gate is reachable without it; it is a live-demo quality call more than a gate call.
   Alternative: leave the loop as designed and document the miss on `/evals`.
3. **Spaces + worker + RAG (gate: none — Atlas and OpenAI key are live).** Per TECHNICAL.md items
   6–8 and SPEC §5.4: `POST /spaces`, upload → GridFS → `jobs` row → 202 in < 300 ms p95; worker
   as a **child process** polling `jobs` (Kurt's decision), parse (PDF text extraction — reuse it
   later for web PDFs in `fetch_page`), chunk, embed with `text-embedding-3-small`, write
   `chunks`; read-your-write probe before `indexed`; `$vectorSearch` behind the existing
   `vectorBackend()` switch, hybrid with `$search` + RRF; `search_documents` tool with page
   locators; `mode=auto` routing. Recall@5 ≥ 0.70 on the provided gold set. Delegate the build
   from a written spec; review inline.
4. **Deep search (gate: step 3 for docs, none for web).** `plan_research` on Haiku, `plan` event
   before any retrieval (p95 < 4 s), 3–6 sub-questions, fan-out with bounded concurrency (start
   at 3), every step and source tagged with its sub-question, one merged citation numbering,
   `DEEP_DAILY_CAP` reserved atomically → 429 `{error, resetsAt}`. **Validate the plan JSON and
   refund the reservation if planning fails before any retrieval** — the peer's build charged a
   credit for a planner JSON error. Deep must read ≥ 2× the distinct sources of quick.
5. **Regenerate `runs/failing/`** with a real provider failure (invalid Tavily key), per AGENTS.md.
6. **Bench, deploy, eval (gate: steps 3–4).** `npm run bench` locally against `localhost:8787`
   first; Q-4 spend estimate from measured cost before the full run. Deploy: gateway and agent
   may both go on Vercel (the peer did; the rubric allows Fly *or* Vercel) — one fewer account
   than Fly; agent must not be publicly reachable, gateway serves `/` and `/evals`. Then the eval
   skill against the deployed gateway; `/evals` renders design answers, run notes, and the two
   trajectories read end to end.
7. **After eval, if time remains:** OpenRouter adapter (OpenAI-compatible streaming with tool
   calls) behind `LLM_PROVIDER=openrouter`, and a published A/B of Haiku vs GLM-5.3-flash vs Kimi
   K2.6 on the same bench as an `/evals` section. Prices pulled 2026-09-14 per MTok in/out:
   GLM-4.6 $0.43/$1.75 · GLM-5.3 $1.40/$4.40 · GLM-5.3-flash $0.15/$0.50 · Kimi K2.6 $0.95/$4.00
   · Kimi K3 $2.65/$13.28 · Haiku 4.5 $1/$5 · Sonnet 5 $2/$10.
8. Update `DESIGN.md` only where the build diverged (synthesis model, TTFT path, Vercel-for-both).

**Lessons from the peer review (Michal Olczak, lumina-web-sage.vercel.app, reviewed 2026-09-14):**
- 85/100 automated, every SLA green in the report; live: TTFT = total time (no token streaming),
  10–20 s on cold web questions; 94k–307k input tokens per quick answer (whole pages in the
  prompt); one quick answer cost $0.0616 > the $0.05 cap; same date defect (model wrote "March
  2026" into its query); "Canberra" answered twice with no citation and a disclaimer.
- Deep: a planner JSON error returned 502 and still consumed a deep credit; on retry, plan
  streamed first, sources tagged by sub-question, honest "what is still unknown" section — good.
- The `quality/rules.json` P2 "precedent" rule fails in their report and the gate still passes;
  two gates show non-zero exit codes and pass. **The TODO-precedent conflict does not block
  submission. Stop worrying about it.**
- Bench TTFT p95 2.4 s came from a warm-cache, doc-heavy workload; do not equate it with live TTFT.
- Their sources snippets are ~700 chars (ours max 300); both gateway and agent ran on Vercel arn1.
- What we have that they don't: token streaming, ranked 1,500-char passage windows (5–12k tokens
  per answer), the date line. Name these on `/evals`.

**Open questions, by person:**
- *Kurt:* TTFT loop change (step 2) — go / document-the-miss; after the A/B, whether Sonnet stays
  for synthesis; Q-4 spend ceiling before the full bench; Q-5 which URL is submitted (deferred);
  Codex's future scope; optional wargame (declined for now).
- *Codex:* nothing requested.
- *Claude:* none blocking.

**Newly dropped files, not yet read by Claude — do not assume their content:** none.

**Start by:** confirm the files above were read → 3–5 sentence state summary → ask the gating
question ("Start with the Sonnet-for-synthesis A/B as planned, or go straight to spaces/worker/RAG
and run the A/B later?") → offer two options: (a) A/B first (≈ 1 hour, ≈ $0.25, informs every
later prompt); (b) Week 2 build first, A/B at the end of the day on a bigger question set.

---

## Session log

**Files created/modified (all under `Assignment_1_LUMINA/`):**
- `Claude_Build/.env` (Kurt; never committed): all keys + Atlas URI. Claude fixed a missing
  `MONGODB_URI=` prefix and removed the stale commented placeholder line.
- `Claude_Build/.env.example`: `LLM_MODEL=claude-haiku-4-5`; documented `EMBEDDING_PROVIDER`.
- `backend/agent/src/env.ts`: embedding provider defaults to `openai`, inherits `fake` only when
  `LLM_PROVIDER=fake` (was inheriting `anthropic` → boot crash).
- `backend/agent/src/loop/prompts.ts`: `todayLine()` in both system prompts; quick budget (two
  searches, two fetches) in the research prompt; `PromptContext.now` for tests.
- `backend/agent/src/loop/quick.ts`: per-call `llm usage` debug log (input/output/cacheRead/
  cacheWrite) with the 4,096-token Haiku cache-minimum note.
- `backend/agent/src/loop/sources.ts`: `toPassages` ranks by query-term overlap, `PASSAGE_LIMIT = 8`.
- `backend/agent/src/tools/fetch-page.ts`: HTML/plain-text only by content-type, `%PDF-`/NUL sniff.
- `backend/agent/bin/ask.py` (new): one-question harness. `docs/peer-review/…json` (new).
- Board: `CLAUDE-067` (this close); `board/STATE.md` refreshed. Memory: `lumina-week1-state`
  updated, `kurt-close-before-compaction` new. Brain: `projects/fde-cohort2-lumina.md`.

**Decisions made:** stay on Haiku for now, explore Sonnet 5 for synthesis (Kurt); OpenRouter/GLM/
Kimi deferred to after eval as a measured refactor (Kurt); Atlas M0 in us-east-1 replacing an
accidental M10 (Kurt terminated it); do not pad prompts to reach the cache minimum (per prior
resume); synthesis reads the top 8 passages by overlap (Claude, tuning); fetch_page refuses
non-HTML (Claude, defect fix); quick budget lives in the prompt for now — a loop-level soft cap
is open if it drifts.

**Explicitly NOT decided:** the TTFT loop change (step 2); whether Sonnet stays for synthesis;
whether to ever switch the research model; Q-4 spend ceiling; Q-5 submitted URL; Codex scope;
wargame; deploy target for agent + gateway (Vercel-for-both now allowed by evidence).

**Mid-session direction from Kurt:** "get this done properly" on Mongo; long cluster names help
him remember purpose; discuss model change (Haiku vs vision for PDFs) → resolved: PDFs need text
extraction not vision; review the peer submission in detail; "continue on our current path and
also explore Sonnet"; close before compaction, "create a robust plan for the next session".

**Spend this session (all-in, approx.):** Anthropic ≈ $0.30 (≈ 12 real asks + probes); Tavily
≈ 25 credits; OpenAI ≈ $0; Atlas M10 for ~40 min ≈ $0.06 before termination; peer site: 7
questions, 2 deep credits, no writes beyond threads under a throwaway user id.

**Commits (`lozierk/FDE-Cohort2`, `main`):** close commit follows this file (hash in the close
message on the board and in the chat summary).

---

## Session closed — 2026-09-14 13:20 ET

**State:** Week 1 loop running on real providers and Atlas, tuned on five questions; Week 2
(spaces/worker/RAG, deep, deploy, eval) not started; Sonnet synthesis A/B queued first. Four days
to the deadline.

**Resume by:** pasting the Quick re-start prompt into a new Claude Code session opened in
`/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/`.

**Do not:**
- Do not re-open the Haiku decision wholesale or the wargame; Sonnet is for the synthesis call
  only unless Kurt says otherwise; OpenRouter waits until after eval.
- Do not pad prompts to reach a cache minimum; do not send PDFs to the model as documents in the
  quick loop — extract text.
- Do not equate bench TTFT with live TTFT; the bench is warm-cache and doc-heavy.
- Do not edit the starter's provided folders, ever; do not relax any threshold or cap; do not
  raise `MAX_TOOL_CALLS` or the wall clock to pass a gate.
- Do not `cat`, print, or copy `.env` or any key; check existence with `test -f` only. If a masked
  check would still expose a fragment, use a stricter mask.
- Do not build the worker on the request thread; it stays a child process polling `jobs`.
- Do not charge a deep credit when planning itself fails before retrieval.
- Do not edit `board/STATE.md` without claiming the lease; never edit a message file or the archive.
- Do not contact Kurt by Telegram unless something is blocking and jointly agreed.
- Do not guess timestamps; use `TZ=America/New_York date`.
- Do not let the session drift toward auto-compaction; recommend `/close resume` at the next
  checkpoint around 25–35% context.
