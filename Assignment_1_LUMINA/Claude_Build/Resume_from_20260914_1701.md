# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-14 17:01 ET · **Session:** 6 (opened 15:34, closed 17:01 ET, 2026-09-14) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260914_1527.md`

**TLDR:** Week 2 is complete. Part B (deep search) was built from its spec by opus-specialist,
reviewed in parallel (read-only opus-specialist + Claude), patched, measured on Atlas with real
providers on all four bench deep questions, and committed (`ae3e72c`). All phase 4 gates pass:
plan 2.4–3.5 s, sources 2.7–7.0× quick, cost ≤ $0.14, wall ≤ 48 s, 429 with `resetsAt` on the
sixth ask, deep under a 60-page ingest inside the idle numbers. DESIGN.md v1.1 (`4d84667`)
describes the build as it runs; `runs/failing/` holds a real failing run; Atlas is clean. Next is
the Sonnet-for-synthesis A/B, then Kurt's web-TTFT call, then bench → deploy → eval.
**Deadline Fri 2026-09-18, three and a half days.**

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260914_1701.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## Detailed starter prompt (fallback)

**Context.** Kurt is in Hamza Farooq's FDE bootcamp, cohort 02. Assignment 1 is LUMINA, a
Perplexity-style search agent. Claude builds solo in `Claude_Build/`; Codex is paused. The course
provides the UI, zod contract, benchmark, grader, gold set, and quality gate; we build only
`backend/agent/` and `backend/gateway/`. Editing a provided folder (`web/ packages/contract/
benchmark/ eval/ quality/ scripts/`) is an automatic fail. The submission is one Vercel URL whose
`/evals` page renders a real eval report against the deployed app. Deadline Fri 2026-09-18.

**Working-style reminders.** Confirm understanding before multi-step work. Review Kurt's thinking
on consequential decisions (he decides model and loop design; you recommend with numbers, and he
approves fast when the numbers are there). One source at a time with checkpoints. Don't fabricate;
say "I don't know". Converge, don't repeat. Orchestrate per CLAUDE.shared.md: delegate a written
spec to opus-specialist, run a read-only opus-specialist review in parallel with your own review,
patch inline, measure yourself. Kurt is never the relay between agents, except for Atlas
multi-collection deletes, which the classifier blocks: write the script with the Write tool and
hand him a `! cd … && npx tsx …` line. Kurt reads the context percentage himself; recommend
`/close resume` at a checkpoint and list cheap finish-ups he could take first.

**Files to read first, in order:**
1. `MESSAGE_BOARD.md` then `board/STATE.md` (assignment root) — entry point and current state.
2. `board/read.sh claude list` then `print` — unread messages; ack with the printed token.
3. `Claude_Build/DESIGN.md` v1.1 — the system as it runs now, including deep search. Read
   Communication and Trade-offs 6–8 before touching the loop.
4. `Claude_Build/backend/agent/src/config/model.ts`, `src/env.ts`, `src/providers/anthropic.ts`,
   `src/loop/quick.ts` (synthesis call at the end), `src/loop/deep.ts` (synthesis call, phase 3),
   `src/routes/health.ts` or wherever `/health` builds its body — the files the Sonnet A/B touches.
5. `Claude_Build/backend/agent/src/loop/prompts.ts` — `synthesisSystemPrompt`, the deep structure
   block, and the planner prompt with its token-budget comment.
6. `Claude_Build/docs/week2-deep-build-spec.md` §1 and §8 — what the bench grades for deep and the
   acceptance already run; `docs/week2-rag-build-spec.md` §1 for the recall/grounding rules.
7. `Claude_Build/backend/agent/bin/ask.py` (`--depth deep`), `upload.py`, `recall.py` — harnesses.
8. `Claude_Build/fixtures/README.md` — the pinned 60-page PDF and its hash.

**Where we paused.** Working tree clean at `4d84667` (`ae3e72c` part B, `4d84667` DESIGN v1.1,
close commit follows). Local services stopped (`pgrep -f "tsx src/index.ts"` empty). `.env`
complete (Anthropic workspace-scoped key, Tavily, OpenAI, Atlas URI, `RATE_LIMIT_PER_MINUTE=300`).
Atlas `lumina-claude` holds only the bench Space `spc_mu1m6az2139fzn` (user `kurt-test`: four
gold corpus files + `bench-60p.pdf`, all `indexed`, 101 chunks) and its five finished job rows;
the deep-under-ingest test Space was deleted by Kurt (script) and its job row by Claude. Deep
credits spent today on throwaway users only (`accept-deep` 5/5, `builder-deep` 3/5,
`plan-tune` 2/5); they reset at 2026-09-15T00:00Z. `runs/failing/req_793296f3-0fe.json` is a
real failing run (web_search ok, then Anthropic 401 → `terminated: error`); `runs/` (ignored by
git) holds only `done` runs.

**How to run locally (from `Claude_Build/`):**
```
(cd backend/agent  && LOG_LEVEL=debug npx tsx src/index.ts &)   # agent :8000 + forked worker; reads ../../.env
(cd backend/gateway && npx tsx src/index.ts &)                   # gateway :8787
curl -s localhost:8787/health
python3 backend/agent/bin/ask.py "question" /tmp/q.json                 # quick web ask
python3 backend/agent/bin/ask.py --depth deep "question" /tmp/d.json    # deep: plan, per-sub sources, distinct sources
python3 backend/agent/bin/upload.py eval/gold/corpus/*.pdf eval/gold/corpus/*.md   # new Space, waits for indexed
python3 backend/agent/bin/recall.py <spaceId>          # 39 gold questions, recall@5 the bench's way
```
Process env beats `.env` (dotenv does not override): `ANTHROPIC_API_KEY=bad npx tsx src/index.ts`
fakes a provider failure. Tests: `npm test` in `backend/agent` (76) and `backend/gateway` (9);
`npm run typecheck`. Stop with `pkill -f "tsx src/index.ts"`.

**Measured this session (Atlas, real providers, mode web, fresh threads, bench deep questions):**

| Question | Plan ms (≤ 4000) | Sub-qs | Distinct sources deep / quick (≥ 2.0×) | Cost (≤ $0.35) | Wall s (≤ 90) |
|---|---|---|---|---|---|
| 1 BM25 vs dense | 3050 | 5 | 35 / 5 = 7.0× | $0.137 | 47.9 |
| 2 Perplexity cost, first prompt | 4856 | 5 | 31 / 9 = 3.4× | $0.114 | 38.5 |
| 2 after planner-prompt cut | 3527 | 4 | 24 / 9 = 2.7× | $0.089 | 36.3 |
| 3 SSE vs WebSockets | 3058 | 6 | 40 / 9 = 4.4× | $0.127 | 35.7 |
| 4 Atlas vs vector DB, first prompt | 4680 | 6 | 26 / 5 = 5.2× | $0.094 | 38.3 |
| 4 after planner-prompt cut | 3028 | 4 | 23 / 5 = 4.6× | $0.086 | 36.7 |
| 1 during 60-page ingest | 2375 | 4 | 29 / 5 = 5.8× | $0.102 | 31.8 (idle 47.9) |

Every retrieval step and every source carried `subQuestion`; `plan` preceded the first retrieval
on every run; sixth deep ask on the same user → `429 {resetsAt}`; the fixture reached `indexed`
in 11.9 s (60 chunks, 60 pages, accept 210 ms). Planner output 330–400 tokens → ~245 after the
prompt cut. **Plan-latency headroom is thin (~12% on the worst sample); Haiku's throughput
varies run to run.** If the bench brushes 4 s, the next lever is fewer sub-questions by default
(the prompt already says four is usual) or shorter questions, never a raised cap.

**Immediate goals for next session, in order, with gates:**

1. **Sonnet 5 for synthesis A/B (gate: none; Kurt approved exploring).** Add
   `LLM_MODEL_SYNTHESIS` (env, default = `LLM_MODEL`), `providers.synthesisLlm`, per-model rates in
   `config/model.ts` (Sonnet 5 input/output rates must come from the live pricing page, not
   memory — load the `claude-api` skill), `/health` names both models when they differ, cost
   accounting per call uses the model that ran. Both `quick.ts` and `deep.ts` synthesis calls use
   it; planner and research stay on Haiku. Run the five web smoke questions plus five gold docs
   questions plus two deep questions on both; table TTFT, cost, dangling citations, and a
   read-aloud quality judgment; **Kurt decides.** ≈ $0.50. Sonnet-builder can do the wiring
   from this paragraph; measure yourself.
2. **Web-mode TTFT — Kurt's call, present once (idle web TTFT today 4.7–13 s on fresh questions
   vs 2.5 s p95 gate).** Docs mode set the precedent (answer from the loop's own preflight,
   TTFT 1.6 s). Web equivalent: preflight search without raw content, fetch top 3 in parallel
   with a 3 s timeout, straight to synthesis when ≥ 2 pages have text, else the research turns.
   Expected ≈ 3 s cold. Alternative: document the miss on `/evals`. Present the two options with
   the numbers above and stop.
3. **Q-4 spend ceiling (Kurt) before any bench run.** Estimate from measured costs: quick
   $0.011–0.032, docs $0.002, deep $0.086–0.137, embeddings negligible; the bench runs
   `workload.web_queries` (see `benchmark/sla.json`), the smoke set, the gold recall set, four
   deep questions quick+deep, and a cap probe (5 more deep asks). Put the number in front of Kurt.
4. **Bench locally** (gate: 3): `npm run bench` against `localhost:8787` with
   `RATE_LIMIT_PER_MINUTE=300`; then `node quality/check.mjs .` (expect A3 warnings on deep runs:
   consecutive preflight searches; A2 must be clean because `runs/failing/` is outside `runs/`).
5. **Deploy** (gate: 4 green): agent on Fly (private; the worker is a forked child process, so
   Vercel is out for the agent); gateway on Fly or Vercel — Kurt's call; set
   `RATE_LIMIT_PER_MINUTE=300` and every key in the deployed env; `/health` must name model,
   search, vector backend. Then the eval skill against the deployed gateway; `/evals` renders
   design answers, run notes, and the two trajectories (one successful, the failing one).
6. **After eval, if time remains:** OpenRouter adapter and the Haiku vs GLM-5.3-flash vs Kimi
   K2.6 A/B as an `/evals` section (prices in `Resume_from_20260914_1320.md`).

**Lessons from this session worth carrying:**
- Delegate a written spec, review in parallel, patch inline: the builder's own tests caught two
  real bugs (25 steps under a cap of 24; concurrent overrun), the review caught two more (pool
  not stopping on failure; `cap` on finished work fails quality rule A2, which the bench itself
  never reads).
- A quality rule can bite where the bench does not: `quality/check.mjs` A2 fails any run log not
  terminated `done`, and `eval/build-report.mjs` promotes that to a red line. Derive budgets so
  finished work is never reported as capped.
- First paint is output tokens: 330–400 planner tokens ran 3.0–4.9 s; ~245 ran 2.4–3.5 s. Say
  "fifteen-word questions, six-word reasons" rather than "be brief".
- A denied Bash command runs none of its parts; write files with the Write tool, then run.
- Don't reopen a tested part-A rule for a cosmetic gain (stable source numbers vs contiguous):
  I tried, then reverted; DESIGN.md trade-off 8 records why.

**Open questions, by person:**
- *Kurt:* Sonnet stays or not after the A/B (goal 1); web-mode TTFT change (goal 2); Q-4 spend
  ceiling (goal 3); gateway deploy target (Fly vs Vercel) and Q-5 which URL is submitted;
  Codex's scope.
- *Codex:* nothing requested.
- *Claude:* none blocking.

**Newly dropped files, not yet read by Claude — do not assume their content:** none.

**Start by:** confirm the files above were read → 3–5 sentence state summary → ask the gating
question ("Start the Sonnet synthesis A/B now, or present the web-TTFT options first?") → offer
two options: (a) A/B now (≈ 1 hour, ≈ $0.50; wiring by sonnet-builder, measurement by Claude,
decision by Kurt); (b) web-TTFT decision first (10 minutes of Kurt's time; if he approves the
change it is ≈ 1 hour of build and re-measure before the A/B so both are measured on the loop
that ships).

---

## Session log

**Files created/modified (all under `Assignment_1_LUMINA/Claude_Build/`):**
- Part B build (commit `ae3e72c`, 25 files): new `src/loop/{research,deep,planner}.ts`,
  `src/store/deep-quota.ts`, `test/deep.test.ts` (12 tests); edited `src/loop/{quick,sources,
  prompts}.ts`, `src/routes/{ask,threads}.ts`, `src/store/index.ts`, `src/runlog.ts`, `src/env.ts`,
  `src/config/model.ts`, `src/providers/{llm,anthropic,fake-llm}.ts`, `src/tools/{types,web-search,
  fetch-page,search-documents}.ts`, `src/sse.ts`, `test/routes.test.ts`, `bin/ask.py`,
  `.env.example` (`DEEP_CONCURRENCY=3`, `DEEP_SUB_TOOL_CALLS=4`, `DEEP_PASSAGES_PER_SUB=4`,
  `DEEP_PASSAGE_LIMIT=20`). Review fixes applied inline: pool stops on first failure (local
  `AbortController` chained to the request signal, emit guard, `Promise.allSettled`); SSE
  write-after-end guard; per-sub budget `min(DEEP_SUB_TOOL_CALLS, floor((24-1)/plan.length))`;
  credit reserved after both thread and Space 404s; planner prompt cut (fifteen-word questions,
  six-word reasons, four sub-questions usual); tests for mid-fan-out failure (HTTP + in-process),
  forced `toolChoice`, six-question full spend, wall-clock cap. Tests 64 → 76.
- `DESIGN.md` v1.1 (commit `4d84667`): sonnet-writer draft from Claude's fact list, reviewed; one
  unverified route claim softened.
- `runs/failing/req_793296f3-0fe.json` (real failing run; git-ignored); the fake one removed.
- Board: `CLAUDE-069` (this close); `board/STATE.md` refreshed. Memory: `lumina-week1-state`,
  `lumina-env-and-atlas-guardrails`, `kurt-close-before-compaction` updated. Brain:
  `projects/fde-cohort2-lumina.md` session 6 entry.

**Decisions made:** part B before the Sonnet A/B (Kurt); the Sonnet A/B waits for a fresh
session (Kurt); planner prompt shortened for the 4 s first-paint gate at the cost of one axis on
question 2's plan (Claude, numbers reported); per-sub budget derived from plan size (Claude, from
quality rule A2); stable source numbering kept, contiguity not enforced (Claude, reverted own
change; part A rule stands); test Space deleted (Kurt ran the script), leftover job row deleted
(Claude, single row).

**Explicitly NOT decided:** Sonnet for synthesis; web-mode TTFT change; Q-4 spend ceiling; Q-5
URL; gateway deploy target; Codex scope.

**Mid-session direction from Kurt:** "Part B deep search now"; at 19% context, "is there anything
you want to finish or update in this session, we can go a bit longer"; "Go on 1 through 3, delete
the Space and we will leave the Sonnet A/B work for a fresh session"; ran the delete script
himself via `!`; then `/close resume`.

**Spend this session (approx.):** Anthropic ≈ $1.10 (builder ≈ $0.30: 2 deep + 2 quick; Claude
≈ $0.80: 9 deep + 4 quick + 1 failing); Tavily ≈ 30 credits; OpenAI embeddings < $0.01; Atlas M0
free. Delegated agents: opus-specialist build (≈ 21 min), opus-specialist review (≈ 7 min),
sonnet-writer DESIGN.md (≈ 2 min).

**Commits (`lozierk/FDE-Cohort2`, `main`):** `ae3e72c` part B · `4d84667` DESIGN v1.1 · close
commit follows this file.

---

## Session closed — 2026-09-14 17:01 ET

**State:** Week 2 complete and measured; DESIGN v1.1; failing run real; Atlas clean; no deploy
yet. Three and a half days to the deadline.

**Resume by:** pasting the Quick re-start prompt into a new Claude Code session opened in
`/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/`.

**Do not:**
- Do not re-open Haiku for planning/research, the same-page merge, the docs-mode preflight path,
  or stable (gapped) source numbering; Sonnet is for the synthesis call only unless Kurt says
  otherwise; OpenRouter waits until after eval.
- Do not read `.env` by any means (deny rule; `test -f` only; append-only writes are allowed;
  process env overrides work).
- Do not run multi-collection deletes on Atlas yourself; write the script, hand Kurt the `!` line.
- Do not edit the starter's provided folders, ever; do not relax any threshold or cap; do not
  raise `MAX_TOOL_CALLS_DEEP`, `MAX_WALL_CLOCK_SEC_DEEP`, `DEEP_DAILY_CAP`, `DEEP_SUB_QUESTIONS_*`,
  or the quick caps to pass a gate. The rate limit is ours, not a gate, and is already set.
- Do not put `plan_research` into `ALL_TOOLS` or `toolsForMode()`; do not emit `plan` after any
  retrieval; do not charge a deep credit when planning fails before retrieval; do not refund one
  after retrieval.
- Do not undo the derived per-sub budget or the planner token cuts without re-measuring plan
  latency on all four bench deep questions.
- Do not let a failing run sit in `runs/`; it lives in `runs/failing/` (rule A2 vs P1).
- Do not build the worker on the request thread; do not deploy the agent to Vercel.
- Do not equate bench TTFT with live TTFT; do not pad prompts to reach a cache minimum.
- Do not edit `board/STATE.md` without claiming the lease; never edit a message file or the archive.
- Do not contact Kurt by Telegram unless something is blocking and jointly agreed.
- Do not guess timestamps; use `TZ=America/New_York date`.
- Do not let the session drift toward auto-compaction; recommend `/close resume` at a checkpoint
  and list cheap finish-ups Kurt could take first.
