# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-14 21:19 ET · **Session:** 8 (opened 18:17, closed ≈ 21:25 ET, 2026-09-14) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260914_1812.md`

**TLDR:** The local bench is green except for one documented miss. Four full runs of the course
bench (≈ $4.60) found four real defects that no hand measurement had seen, and all four are fixed,
tested and committed (`e8e0ed2`): the bench sends 40 web queries down ONE thread and the preflight
fast path fired only on a fresh thread; "Remember this preference…" was answered from a web search
so nothing was saved; Tavily raw content is markdown and snippets opening with link syntax never
matched the grader's HTML; the gateway turned the agent's bodiless 204 on DELETE /memory into a
502. Bench 4: **22/22 gates, 15/16 SLA, grounding 0.981, memory ✓; TTFT p95 4.1 s vs 2.5 s is the
miss to state plainly** (36–38 of 40 web answers under 1 s; p95 over 40 is the 2nd slowest; one slow
model first token sets it). Deploy is decided and prepared (`DEPLOY.md`, `7beeafb`): agent private on
Fly, gateway public on Fly, UI static on Vercel; **blocked only on Kurt's `fly auth login` and
`vercel login`**. Q-4 ceiling raised to **$15**. Write-up drafted (`docs/WRITEUP.md` + private
Notion page). Tests: agent 125, gateway 12. **Deadline Fri 2026-09-18, three days.**

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260914_2119.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## Detailed starter prompt (fallback)

**Context.** Kurt is in Hamza Farooq's FDE bootcamp, cohort 02. Assignment 1 is LUMINA, a
Perplexity-style search agent. Claude builds solo in `Claude_Build/`; Codex is paused through
submission. The course provides the UI, zod contract, benchmark, grader, gold set, and quality
gate; we build only `backend/agent/` and `backend/gateway/`. Editing a provided folder (`web/
packages/contract/ benchmark/ eval/ quality/ scripts/`) is an automatic fail. The submission is one
Vercel URL whose `/evals` page renders a real eval report against the deployed app. Deadline Fri
2026-09-18.

**Working-style reminders.** Confirm understanding before multi-step work. Review Kurt's thinking
on consequential decisions (he decides platform, model and spend; you recommend with numbers, and
he approves fast when the numbers are there — tonight he raised the ceiling unasked "so you have
full solid runs"). One source at a time with checkpoints. Don't fabricate; say "I don't know".
Converge, don't repeat. Orchestrate per CLAUDE.shared.md: delegate a written spec to
sonnet-builder or opus-specialist, review what comes back, patch inline, measure yourself. Kurt is
never the relay between agents, except for `.env`-touching and Atlas multi-collection deletes,
which the classifier blocks: write the script, hand him a `!` line. Never read `.env` by any means
(a Bash line that merely mentions it is denied whole); appending works. Kurt reads the context
percentage himself; recommend `/close resume` at a checkpoint and list cheap finish-ups.

**Files to read first, in order:**
1. `MESSAGE_BOARD.md` then `board/STATE.md` (assignment root) — entry point and current state;
   Q-4 is now $15.
2. `board/read.sh claude list` then `print` — unread messages; ack with the printed token.
3. `Claude_Build/DEPLOY.md` — the exact command order for tomorrow, with the lines Kurt runs
   marked. This is the next work step.
4. `Claude_Build/DESIGN.md` v1.4 — the system as it runs; the Communication section now carries
   the four bench findings and the TTFT distribution argument.
5. `Claude_Build/reports/bench.json` (bench 4) and `reports/bench.local-{1,2,3}.json` — the
   four local runs; `runs/` holds bench 4's logs, `runs.local-{1,2,3}/` the earlier ones.
6. `Claude_Build/docs/WRITEUP.md` — the draft write-up; every ⟦DEPLOYED⟧/⟦EVAL⟧/⟦REPO⟧/⟦VIDEO⟧
   placeholder is what the deployed run fills.
7. `Claude_Build/backend/agent/src/loop/{standalone,research,quick}.ts` — the preflight rules as
   they now stand (standalone check, memory recall first step, preflight fetch fallback).
8. `Claude_Build/scripts-local/{sort-failing,grounding-audit}.mjs` — ours: restore
   `runs/failing/` after a Mongo export; re-score grounding with the bench's own matcher.

**Where we paused.** Working tree clean after the close commit. Local services STOPPED. `.env`
unchanged since session 7 (complete). Atlas `lumina-claude` holds the bench Spaces from four runs
(the bench creates one per run; harmless) and tonight's `searchCache` rows (6 h TTL; the
web-search tool re-cleans cached markdown, so they need no purge). Deep credits: the bench uses
a fresh deep user per run, so nothing waits on the 00:00Z reset. Fly and Vercel CLIs installed,
**neither logged in**; no Docker locally, so Fly builds remotely (`--remote-only`). The Notion
write-up is a private draft: https://app.notion.com/p/3dc4f6430102817ca7e1ffec97a03650.

**How to run locally (from `Claude_Build/`):**
```
(cd backend/agent  && LOG_LEVEL=info npx tsx src/index.ts &)    # agent :8000 + forked worker
(cd backend/gateway && npx tsx src/index.ts &)                  # gateway :8787
curl -s localhost:8787/health          # model "claude-haiku-4-5; deep synthesis: claude-sonnet-5"
mv runs runs.local-N                   # so the quality check reads only the next run
node benchmark/bench.mjs               # full run ≈ 10 min, ≈ $1.10 → reports/bench.json
node quality/check.mjs .               # expect 0 errors, 2 warnings (A3 deep thrash, P2)
node scripts-local/grounding-audit.mjs --limit 75 --user bench   # which citations fail and why
pkill -f "tsx src/index.ts"
```
Tests: `npm test` in `backend/agent` (125) and `backend/gateway` (12); `npm run typecheck`.
Loop tests import `test/mongo-fallback.ts` first so the memory-recall step hits an in-memory
Mongo, not Atlas.

**Measured tonight (local, bench 4 unless noted):**

| Metric | Run 1 | Run 4 | Gate |
|---|---|---|---|
| TTFT p95 (web) | 12.8 s | **4.1 s** | 2.5 s — documented miss |
| Answer p95 | 15.0 s | 6.6 s | 12 s |
| Search cache hit rate | 10% | 97.5% | 50% |
| Citation grounding | 0.913 | 0.981 | 0.95 |
| Cost per quick answer | $0.0175 | $0.0036 | $0.05 |
| Memory saved / recalled / deleted | ✗ ✗ ✗ | ✓ ✓ ✓ | — |
| Rubric gates | 17/22 | 22/22 | — |
| Deep: plan p95 / answer p95 / sources ratio / cost | 3.4 s / 51 s / 4.4× / $0.136 | 2.9 s / 64 s / 5× / $0.144 | 4 s / 90 s / 2× / $0.35 |

Web TTFT distribution, run 4: 36 of 40 under 1 s, p50 0.66 s; the four above: 1.5, 3.0, 4.6, 8.9 s
(8.9 s = a query whose search extracted no text and a research turn; 4.6 s = same; 3.0 and 8.2 s in
run 4 were cache HITS with two tool calls — a slow model first token). Any two outliers set the p95.

**Immediate goals for next session, in order, with gates:**

1. **Logins (gate: Kurt).** `! fly auth login` and `! vercel login`. Then
   `fly config validate -c fly.agent.toml` and `-c fly.gateway.toml` (needs the login).
2. **Deploy (gate: 1), per `DEPLOY.md` §1–3.** Agent: `fly apps create lumina-claude-agent`, Kurt
   runs the secrets import line, `fly deploy -c fly.agent.toml --remote-only`, check logs for
   "agent up" and "jobs worker child started", confirm `fly ips list` is empty. Gateway: create,
   deploy, `curl …/health` names both models + tavily + atlas. UI: build `@lumina/contract` and
   `@lumina/web` with `VITE_API_URL=https://lumina-claude-gateway.fly.dev`, copy `web/dist` +
   `web/vercel.json` to `deploy/web/`, `vercel --prod --yes --name lumina-claude` there (Kurt links
   the project on first run), then `fly secrets set CORS_ORIGINS=https://<ui>.vercel.app -a
   lumina-claude-gateway`. Names, if taken, change only the suffix. Q-5 is settled: the Vercel URL.
3. **Bench against the deployed gateway (gate: 2), per `DEPLOY.md` §4.** `mv runs runs.local-4`,
   `node benchmark/bench.mjs --target https://lumina-claude-gateway.fly.dev`, then
   `node scripts/export-runs.mjs` (Mongo → `runs/`), `node scripts-local/sort-failing.mjs`,
   `node quality/check.mjs .`, `node eval/eval.mjs --deploy-url …`, `node eval/build-report.mjs
   --successful <req> --failing <req>` (a failing run exists only if one happens; the local
   `runs.local-1/failing/` has none — check `runs.local-*/failing/` and Mongo for a real one, e.g.
   session 6's `req_793296f3-0fe` is in the Mongo `runs` collection), open `/evals` on the Vercel
   URL and read every number against `reports/`. Expect the TTFT miss again; say it plainly in the
   run notes. ≈ $1.10 + Tavily credits per run; Q-4 ceiling $15, ≈ $4.60 spent.
4. **Write-up final (gate: 3).** Fill the placeholders in `docs/WRITEUP.md` and the Notion page
   (send the write-up agent the deployed `bench.json`, or do it inline); resolve the three items
   the agent left: repo link (`lozierk/FDE-Cohort2` is private — Kurt decides what to submit), the
   wargame line (its "why" is the agent's inference; Kurt corrects or cuts), `/stats.answers` is
   cumulative per process (a fresh deploy reads clean). Kurt records the video.
5. **If time remains:** OpenRouter adapter and the Haiku vs GLM-5.3-flash vs Kimi K2.6 A/B as an
   `/evals` section (prices in `Resume_from_20260914_1320.md`).

**Lessons from this session worth carrying:**
- **Measure with the grader's harness.** `ask.py` opened a thread per question; the bench does
  not. Four defects in one evening, none visible by hand. The bench's own code (`buildWorkload`,
  `runMemory`, `scoreGrounding`, `fetchPageText` + `stripHtml`) is part of the spec — read it
  before touching ours.
- **A fast path that removes the model's turn removes everything the model did in that turn.**
  Recall and save were model-called tools; the loop now does recall itself and yields the turn
  for an instruction. Check every tool the turn used to call before skipping it.
- **A cache serves the old shape of the data.** The provider-level cleaning did nothing for six
  hours of cached rows; clean at the consumer too.
- **p95 over 40 is the second-slowest value.** Report the distribution; do not buy the gate.
- **Delegating the write-up early paid twice:** the agent read `bench.json` more completely than I
  had and found the memory-gate failure I had scrolled past.

**Open questions, by person:**
- *Kurt:* the two logins; the Fly app names if taken; what repo link to submit (private repo);
  the video; whether Codex gets any scope before Friday (recommendation: no).
- *Codex:* nothing requested.
- *Claude:* none blocking.

**Newly dropped files, not yet read by Claude — do not assume their content:** none.

**Start by:** confirm the files above were read → 3–5 sentence state summary → ask the gating
question ("Have you run `fly auth login` and `vercel login`? If yes, I start `DEPLOY.md` §1 now
and hand you the secrets line; if not, that is the first thing.") → offer two options: (a) deploy
now, top to bottom, bench vs deployed in the same session (≈ 45 min, ≈ $1.20); (b) deploy the
agent and gateway only, verify `/health` and one real ask through Fly, and leave the UI + bench
for a second sitting.

---

## Session log

**Files created/modified (all under `Assignment_1_LUMINA/Claude_Build/`):**
- `7beeafb` deploy prep: `Dockerfile.agent`, `Dockerfile.gateway`, `fly.agent.toml`,
  `fly.gateway.toml`, `.dockerignore`, `DEPLOY.md`, `scripts-local/sort-failing.mjs`, `.gitignore`.
- `e8e0ed2` the four fixes: `src/loop/standalone.ts` (new: `looksStandalone`,
  `looksLikeMemoryRequest`), `src/loop/quick.ts` (preflight on standalone questions; memory
  requests keep the turn; `preflight.memory: true`; memories into the synthesis system prompt),
  `src/loop/research.ts` (memory recall as first step; `preflight()` takes args + unique ids;
  `WEB_PREFLIGHT_FETCHES` fallback), `src/loop/sources.ts` (`SNIPPET_MIN` 160, `looksLikeMarkup`),
  `src/providers/tavily.ts` (`cleanRawContent`), `src/tools/web-search.ts` (clean again on
  register; `citableUrl` host filter), `src/providers/fake-llm.ts` (default script looks for a
  search, not any tool_result), `backend/gateway/src/app.ts` (204 passthrough); tests:
  `standalone`, `raw-content`, `mongo-fallback` (new), `loop` (+3 incl. local-server preflight
  fetch), `sources`, `routes`, `retrieval`, `deep` (updated for the recall step), gateway
  `memory-delete` (new) + stub route. `DESIGN.md` v1.4. `scripts-local/grounding-audit.mjs`.
- Close commit: this file, `docs/WRITEUP.md` (opus-specialist), board `CLAUDE-071`,
  `board/STATE.md` refresh.
- Reports kept: `reports/bench.local-{1,2,3}.json`, `runs.local-{1,2,3}/` (git-ignored).

**Decisions made:** gateway on Fly, agent private on Fly, UI prebuilt static on Vercel from
`deploy/web/` (Claude recommended; Kurt: "either platform"); no run-log volume (Mongo + export);
Codex stays paused (recommended, not yet confirmed); Q-4 ceiling $15 (Kurt); standalone-question
heuristic over a model-turn rewrite (Claude); memory recall as a loop step on every quick run
(Claude); YouTube/login hosts uncitable (Claude); TTFT p95 stays a documented miss (carried).

**Explicitly NOT decided:** Fly app names if the defaults are taken; the submitted repo link;
Codex's scope (Kurt).

**Mid-session direction from Kurt:** "standby… either Vercel or Fly… recommend on the other items"
→ "Run your proposed sequence: local full bench now. I'll do the logins tomorrow morning" →
"commit the deploy prep files" → "I'd like to do a full writeup on Notion similar to [the Viome
Meals report]" → "You can exceed the $10 target bench and increase it to $15 if you need to so you
have full solid runs" → "commit the fixes and then /close resume".

**Spend this session (approx.):** four full local benches ≈ $4.60 by the agent's ledger
(`/stats.costUsdToday` read 2.35 after the key's 20:00 ET reset); Tavily ≈ 60 credits (95%+ cache
hits from run 3 on); grounding audits: no paid calls. Delegated: opus-specialist ×1 (write-up, two
passes, ≈ 10 min).

**Commits (`lozierk/FDE-Cohort2`, `main`):** `7beeafb` · `e8e0ed2` · close commit follows this file.

---

## Session closed — 2026-09-14 ≈ 21:25 ET

**State:** local bench green but for the documented TTFT p95; four defects fixed and tested;
deploy prepared and blocked on two logins; write-up drafted with placeholders. Three days.

**Resume by:** pasting the Quick re-start prompt into a new Claude Code session opened in
`/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/`.

**Do not:**
- Do not re-open the standalone-question heuristic, the memory-recall-first step, the raw-content
  cleaning, the host filter, `SNIPPET_MIN` 160, or the preflight fetch fallback; each has a bench
  number behind it. Do not chase the TTFT p95: report the distribution.
- Do not gate the web preflight on a fresh thread again, and do not skip the model's turn on a
  request that `looksLikeMemoryRequest`.
- Do not read `.env` by any means, and do not put `.env` in any Bash line; append-only writes are
  allowed; process env overrides work. Kurt runs the `fly secrets import` line.
- Do not run multi-collection deletes on Atlas yourself; write the script, hand Kurt the `!` line.
- Do not edit the starter's provided folders, ever — including `web/` (deploy from `deploy/web/`,
  never `vercel` inside `web/`) and `benchmark/sla.json`. Do not relax any threshold or cap.
- Do not exceed the $15 Q-4 ceiling without Kurt; ≈ $4.60 spent; a full bench ≈ $1.10.
- Do not deploy the agent to Vercel or give it a public IP; do not put the gateway on Vercel.
- Do not commit `runs/`, `runs.local-*/`, `reports/`, `deploy/web/` or `.vercel/`.
- Do not run `npm test` in `backend/agent` against Atlas: `test/mongo-fallback.ts` must stay the
  first import of any test that drives the loop.
- Do not move cap runs to `runs/failing/`; only error runs go there (and after a Mongo export, run
  `scripts-local/sort-failing.mjs`).
- Do not put `plan_research` into `ALL_TOOLS` or `toolsForMode()`; do not raise the deep caps.
- Do not edit `board/STATE.md` without claiming the lease; never edit a message file or the archive.
- Do not guess timestamps; use `TZ=America/New_York date`.
- Do not let the session drift toward auto-compaction; recommend `/close resume` at a checkpoint.
