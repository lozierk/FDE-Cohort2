Build a quick working prototype based on this description — visual confirmation, not production code. My global CLAUDE.md ("How I Code") covers the baseline; additionally:

- Flag anything that would need to change for production in a "Production Notes" comment block at the end
- If the description is vague, make reasonable assumptions and state them rather than asking
- When the open question is LOOK or LAYOUT (not logic), build 2-3 radically different
  variants in one file, switchable via a URL param or on-screen toggle — comparing beats
  imagining. Skip this for logic/flow prototypes; one clear path wins there.

$ARGUMENTS
