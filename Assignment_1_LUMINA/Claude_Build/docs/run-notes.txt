Deployed 2026-09-15: agent private on Fly (iad), gateway public on Fly, provided UI static on Vercel. Models: claude-haiku-4-5 for planning, research and quick answers; claude-sonnet-5 for the deep answer only. Search: Tavily with raw content, 6 h cache in Atlas. Vector: Atlas Vector Search + BM25, reciprocal rank fusion. Two eval runs on the grader's path (eval.mjs --deploy-url) against the deployed gateway on 2026-09-15. Run 1 (07:41 ET): gates 0-3 pass, TTFT p95 1.2 s, one miss at gate 4, deep plan p95 4045 ms vs 4000 ms, the slowest of four planner calls, reported as measured. Run 2 (09:48-09:55 ET; a 09:43 attempt stopped at gate 3 because runs/ had been emptied per the runbook, whose order is now corrected) followed one agent release: a 20 s deadline on every tool call (a timed-out tool is a failed step with an error), the Anthropic SDK's silent retries replaced by an explicit policy (at most two retries, waits capped at 2 s, only before the first token, abortable), and retrieved page text wrapped as untrusted content for the model. Run 2: all gates pass, 16 of 16 SLA targets met: TTFT p95 1185 ms, answer p95 4.9 s, deep plan p95 3619 ms, deep answer p95 73.3 s, grounding 0.985, recall@5 30/30, cache hit rate 100%, 0 errors, $0.0033 per quick answer and $0.131 per deep answer. The served report is run 2; run 1 is kept as reports/eval.deployed-1.json and bench.deployed-1.json. Quality: 0 errors, 2 warnings. A3 flags fetch_page thrash on deep runs, which is what parallel sub-questions look like from outside. P2 is left failing on purpose: quality/rules.json is a provided file and its precedents are the starter's TODOs. Real precedents from this build, in the write-up: (A1) 2026-09-14 20:53 ET a provider exception after a successful search ended req_793296f3-0fe as terminated=error with no answer served; (E2) 2026-09-14 Tavily markdown snippets failed grounding against the grader's HTML fetch until raw content was cleaned to plain text; (B2) 2026-09-15 the bench's mode=auto probe spent a model turn on a research step (TTFT 6.9 s / 9.4 s) until auto mode with an answering Space took the docs fast path. The projected monthly cost uses sla.json's placeholder Sonnet prices, not this build's Haiku/Sonnet split; per-answer costs come from the agent's own done events at published Anthropic rates. Run logs: Mongo runs collection exported with scripts-local/export-since.mjs, all deployed runs of 2026-09-15 plus the kept failing run.
