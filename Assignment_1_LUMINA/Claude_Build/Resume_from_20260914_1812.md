# Resume — LUMINA, Claude build (Assignment 1, FDE Cohort 02)

**Date:** 2026-09-14 18:12 ET · **Session:** 7 (opened 17:07, closed 18:12 ET, 2026-09-14) · **Owner:** Kurt Lozier · **Agent:** Claude (Fable 5.1, Claude Code)
**Supersedes:** `Claude_Build/Resume_from_20260914_1701.md`

**TLDR:** Four things settled. (1) Web-mode TTFT: the quick loop now answers straight from the
preflight search when it returned two or more pages of text — cold TTFT 4.7–13 s → 1.8–3.6 s,
warm 0.6–0.9 s; Tavily itself is 1.0–2.3 s cold with or without raw content, so the remaining
miss against the 2.5 s gate (bench p95 ≈ 3 s) is documented, not chased. (2) Tests hardened:
agent 78 → 115, gateway 9 → 11; a real defect fixed on the way — error runs used to land in
`runs/` where quality rule A2 fails them; they now go to `runs/failing/` by construction.
(3) The model split, Kurt's decision from a 12-question-per-arm A/B: **Haiku 4.5 for planning,
research and quick answers; Sonnet 5 for the deep answer only** (`LLM_MODEL_SYNTHESIS_DEEP`,
already in `.env`; DESIGN.md v1.3 trade-off 9; Kurt wants it featured in the final write-up).
(4) **Q-4 spend ceiling $10.00 for the bench and eval cycle**, approved; expected ≈ $3.60.
Next is the local full bench, then deploy on Fly, then the bench against the deployed app and
the eval. Working tree clean at `835f983` + close commit. **Deadline Fri 2026-09-18, three and a
half days.**

---

## Quick re-start prompt

> Resuming LUMINA, Claude build (Assignment 1, FDE Cohort 02).
>
> Please read `~/.claude/CLAUDE.md`, `/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/Claude_Build/Resume_from_20260914_1812.md`, then `MESSAGE_BOARD.md` and `board/STATE.md` in the assignment root, and run `board/read.sh claude list` before responding.
>
> Then confirm you understand the canonical state and propose how to start the next work step.

---

## Detailed starter prompt (fallback)

**Context.** Kurt is in Hamza Farooq's FDE bootcamp, cohort 02. Assignment 1 is LUMINA, a
Perplexity-style search agent. Claude builds solo in `Claude_Build/`; Codex is paused. The course
provides the UI, zod contract, benchmark, grader, gold set, and quality gate; we build only
`backend/agent/` and `backend/gateway/`. Editing a provided folder (`web/ packages/contract/
benchmark/ eval/ quality/ scripts/`) is an automatic fail. The submission is one Vercel URL whose
`/evals` page renders a real eval report against the deployed app. Deadline Fri 2026-09-18.

**Working-style reminders.** Confirm understanding before multi-step work. Review Kurt's thinking
on consequential decisions (he decides model and loop design and spend; you recommend with
numbers, and he approves fast when the numbers are there). One source at a time with checkpoints.
Don't fabricate; say "I don't know". Converge, don't repeat. Orchestrate per CLAUDE.shared.md:
delegate a written spec to sonnet-builder (well-specified) or opus-specialist (design-sensitive),
review what comes back, patch inline, measure yourself; Kurt expects the delegation and checks
for it. Kurt is never the relay between agents, except for Atlas multi-collection deletes, which
the classifier blocks: write the script with the Write tool and hand him a `! cd … && npx tsx …`
line. Never read `.env` by any means (a Bash line that merely mentions it is denied whole);
appending to it works. Kurt reads the context percentage himself; recommend `/close resume` at a
checkpoint and list cheap finish-ups he could take first.

**Files to read first, in order:**
1. `MESSAGE_BOARD.md` then `board/STATE.md` (assignment root) — entry point and current state,
   including the Q-4 gate line.
2. `board/read.sh claude list` then `print` — unread messages; ack with the printed token.
3. `Claude_Build/DESIGN.md` v1.3 — the system as it runs; trade-off 9 is the model split, the
   Communication section has the web-mode preflight paragraph with the TTFT numbers.
4. `Claude_Build/benchmark/sla.json`, `benchmark/bench.mjs` (phases 0–4), `quality/check.mjs`
   (rules A1–A3, P1), `eval/eval.mjs` and `eval/build-report.mjs` — what the bench runs, what
   the quality check reads, what the report needs (`reports/bench.json` from a FULL run; the
   builder refuses a smoke run; A2 fails any non-done log under `runs/`).
5. `Claude_Build/TECHNICAL.md` §§ on running the bench, deploying, and the evals page.
6. `Claude_Build/backend/agent/src/index.ts` (boot, worker fork), `src/env.ts` (every knob),
   `src/providers/index.ts` (the three model knobs and the boot-time rate check),
   `src/routes/health.ts` — what a deploy has to get right.
7. `Claude_Build/backend/agent/bin/ask.py` (`--depth deep`, `--mode docs --space ID`,
   `--user ID`), `upload.py`, `recall.py` — harnesses.
8. `Claude_Build/fixtures/README.md` — the pinned 60-page PDF and its hash (phase 3 needs it).

**Where we paused.** Working tree clean at `835f983` (close commit follows). Local services
STOPPED (`pgrep -f "tsx src/index.ts"` empty). `.env` complete (Anthropic workspace-scoped key,
Tavily, OpenAI, Atlas URI, `RATE_LIMIT_PER_MINUTE=300`, and now
`LLM_MODEL_SYNTHESIS_DEEP=claude-sonnet-5` appended by Claude). Atlas `lumina-claude` holds the
bench Space `spc_mu1m6az2139fzn` (user `kurt-test`, four gold corpus files + `bench-60p.pdf`, all
`indexed`) and its job rows; session 7 asked only against that Space and created no new Spaces.
Deep credits spent today on throwaway users (`ab-haiku` 2/5, `ab-sonnet` 2/5, `ab-split` 2/5,
plus session 6's); they reset at 2026-09-15T00:00Z (20:00 ET today). `runs/` (git-ignored)
holds only `done` runs; `runs/failing/req_793296f3-0fe.json` is the real failing run.

**How to run locally (from `Claude_Build/`):**
```
(cd backend/agent  && LOG_LEVEL=info npx tsx src/index.ts &)    # agent :8000 + forked worker; reads ../../.env
(cd backend/gateway && npx tsx src/index.ts &)                  # gateway :8787
curl -s localhost:8787/health          # expect model "claude-haiku-4-5; deep synthesis: claude-sonnet-5"
python3 backend/agent/bin/ask.py "question" /tmp/q.json                              # quick web
python3 backend/agent/bin/ask.py --mode docs --space spc_mu1m6az2139fzn "q" /tmp/d.json  # docs
python3 backend/agent/bin/ask.py --depth deep --user fresh-name "question" /tmp/x.json  # deep (5/user/day)
node benchmark/bench.mjs --smoke       # five queries, Gate 2 (≈ $0.05)
node benchmark/bench.mjs               # full run (≈ $1.05, ≈ 50 Tavily credits) → reports/bench.json
node quality/check.mjs .               # A1–A3, P1 over runs/
```
Process env beats `.env` (dotenv does not override): `LLM_MODEL_SYNTHESIS_DEEP=claude-haiku-4-5
npx tsx src/index.ts` runs single-model. Tests: `npm test` in `backend/agent` (115) and
`backend/gateway` (11); `npm run typecheck`. Stop with `pkill -f "tsx src/index.ts"`.

**Measured this session (Atlas, real providers):**

| What | Haiku everywhere | Sonnet 5 synthesis | Shipped split |
|---|---|---|---|
| Web quick TTFT, warm cache (5) | 0.6–0.8 s | 0.9–2.0 s | Haiku: 0.9 s |
| Web quick cost, warm / cold | $0.003–0.004 / $0.011–0.012 | $0.008–0.013 / ≈ $0.02 | Haiku |
| Docs TTFT / cost (5 gold, all correct both arms) | 1.1–2.8 s / $0.002 | 1.1–2.5 s / $0.004–0.007 | Haiku |
| Deep: sources cited / cost / wall (2) | 7–9 / $0.072–0.077 / 20–23 s | 13–14 / $0.080–0.093 / 29–31 s | Sonnet: 13 of 17 / $0.076–0.084 / 28–34 s |
| Dangling citations | 0 | 0 | 0 |

Web TTFT before the preflight change: 4.7–13 s cold; after: 1.8–3.6 s cold (median 2.4),
0.6–0.9 s warm; Tavily 1.0–2.3 s cold, same with raw content off (five queries each way).
Bench p95 expectation ≈ 3.0 s vs the 2.5 s gate (half the workload is repeated; p95 over 40
lands on a cold query). Plan first-paint unchanged (Haiku both arms, 1.9–2.6 s this session).

**Immediate goals for next session, in order, with gates:**

1. **Local full bench (gate: none; Q-4 ceiling $10 set).** Start both services, confirm
   `/health` names both models, `node benchmark/bench.mjs` against `localhost:8787`. Expect: TTFT
   p95 ≈ 3 s (a documented miss), everything else green. Then `node quality/check.mjs .`
   (expect A3 warnings on deep runs; A2 must be clean). Read `reports/bench.json`. If something
   red is a real defect, fix, re-run (second run budgeted). Do not relax a threshold or cap.
2. **Deploy (gate: 1 green or understood).** Agent on Fly (private app; child-process worker
   rules Vercel out); gateway on Fly or Vercel — Kurt's call, ask once with the peer evidence
   (both on Vercel is rubric-allowed; a Fly gateway keeps one platform). Deployed env: every key,
   `RATE_LIMIT_PER_MINUTE=300`, `LLM_MODEL_SYNTHESIS_DEEP=claude-sonnet-5`, Atlas URI, `RUNS_DIR`
   on a volume or accept ephemeral logs (decide with Kurt: the eval reads `runs/`). `/health`
   must name model, search, vector backend. Q-5 (which URL is submitted) is decided here.
3. **Bench against the deployed app** (`node benchmark/bench.mjs --target <gateway url>`), then
   `eval/eval.mjs` and `build-report.mjs`; `/evals` renders design answers, run notes, and the
   two trajectories (one successful, the failing one). Say the TTFT miss plainly in the notes.
4. **Final write-up (when Kurt asks; he has asked that the model split be featured).** Sections:
   architecture as it runs (DESIGN v1.3), measured numbers per gate, the model split with the
   A/B table above and the reasoning "pay for the pricier model only where the difference
   shows", the web-mode preflight lesson (TTFT is model turns, not search), failing-run
   placement, what was given up.
5. **If time remains:** OpenRouter adapter and the Haiku vs GLM-5.3-flash vs Kimi K2.6 A/B as an
   `/evals` section (prices in `Resume_from_20260914_1320.md`).

**Lessons from this session worth carrying:**
- Measure before proposing a second lever: Tavily without raw content was the resume's plan B,
  and five queries each way showed no difference. Ten minutes saved an hour.
- A test count is not a score. Kurt read "78 passing" as a score; the honest answer was that
  nothing grades it, and the useful tests are the ones that pin a regression the bench or the
  quality check would catch after deploy. Two of six items were already covered; say so.
- Route by where the difference shows: an A/B across three question sets found the pricier
  model earns its cost on one of them. A per-call, per-model cost ledger is what makes a mixed
  configuration honest.
- Delegated batches work in new files: sonnet-builder wrote 16 tests in four new files while
  Claude edited src and two other test files; no conflicts, one review, one commit.

**Open questions, by person:**
- *Kurt:* gateway deploy target (Fly vs Vercel) and Q-5 which URL is submitted; whether run logs
  on the deployed agent need a volume; Codex's scope.
- *Codex:* nothing requested.
- *Claude:* none blocking.

**Newly dropped files, not yet read by Claude — do not assume their content:** none.

**Start by:** confirm the files above were read → 3–5 sentence state summary → ask the gating
question ("Start the local full bench now, or decide the gateway platform first so the deploy
follows the bench without a pause?") → offer two options: (a) bench now (≈ 10 minutes wall,
≈ $1.05; read the report together, fix only real defects); (b) platform decision first
(five minutes of Kurt's time), then bench, then deploy in the same session.

---

## Session log

**Files created/modified (all under `Assignment_1_LUMINA/Claude_Build/`):**
- `c556ff9` web preflight skip: `src/loop/research.ts` (`WEB_PREFLIGHT_MIN_SOURCES = 2`, skip when
  quick + web + fresh thread + ≥ 2 citable pages), `test/loop.test.ts` (+2), `DESIGN.md` v1.2.
- `43db029` tests for the gates: `src/routes/health.ts` (new router, injectable ping),
  `src/index.ts` (mounts it), `src/runlog.ts` (`runLogDir`: error → `runs/failing/`),
  `test/{health,runlog,fetch-page,web-search,stats,deep-quota}.test.ts`,
  `backend/gateway/test/health.test.ts`.
- `a952b5c` synthesis model: `src/config/model.ts` (rate table, `ratesFor`, `hasRates`,
  `llmCostUsd(u, model)`), `src/env.ts`, `src/providers/index.ts` (`synthesisLlm`, boot rate
  check), `src/loop/research.ts` (`callLlm` takes `llm`, `spend`), `src/loop/{quick,deep}.ts`
  (per-call spend, `model` = the model that wrote the answer), `bin/ask.py` (`--space`),
  `.env.example`, `test/model-rates.test.ts`, `test/{loop,health}.test.ts`.
- `835f983` deep-only synthesis: `LLM_MODEL_SYNTHESIS_DEEP` in env/providers/deep/health,
  `test/providers.test.ts`, `DESIGN.md` v1.3 trade-off 9, `.env.example`.
- `.env`: one appended line `LLM_MODEL_SYNTHESIS_DEEP=claude-sonnet-5` (git-ignored).
- Board: `CLAUDE-070` (this close); `board/STATE.md` refreshed (Q-4 set). Memory:
  `lumina-week1-state` rewritten, `lumina-model-split-decision` new. Brain:
  `projects/fde-cohort2-lumina.md` session 7 entry.

**Decisions made:** web-TTFT Option 1, answer from the preflight (Kurt); Option 2 dropped on
measurement (Claude, numbers reported); all six test items now (Kurt); error runs to
`runs/failing/`, cap runs stay visible (Claude); Sonnet 5 for the deep answer only (Kurt);
Q-4 ceiling $10.00 (Kurt).

**Explicitly NOT decided:** gateway deploy target; Q-5 URL; run-log volume on Fly; Codex scope.

**Mid-session direction from Kurt:** "Go with B, present the web-TTFT options" → "Go with
Option 1, build it and measure" → "give me some ideas to improve on the score '78 passing'" →
"complete all 6 now" and "I expected you to hand off items 3 through 6 to sonnet-builder" →
"Proceed with the Sonnet-synthesis A/B" → "I like the recommendation of Haiku for quick and
Sonnet for deep synthesis only. Brilliant idea, make sure this is noted and described in the
final assignment write up" → "set the ceiling at $10.00 for bench and eval" → `/close resume`.

**Spend this session (approx.):** Anthropic ≈ $1.00 (TTFT measurement 20 quick asks; A/B 24
asks incl. 4 deep; split check 2 deep + 1 quick); Tavily ≈ 40 credits (incl. 10 probe searches);
OpenAI embeddings < $0.01. Delegated agents: sonnet-builder ×2 (tests ≈ 10 min; wiring ≈ 6 min).

**Commits (`lozierk/FDE-Cohort2`, `main`):** `c556ff9` · `43db029` · `a952b5c` · `835f983` ·
close commit follows this file.

---

## Session closed — 2026-09-14 18:12 ET

**State:** Week 2 complete; web TTFT fixed to the search floor; tests 115 + 11; model split
decided and shipped in `.env`; Q-4 ceiling $10 set; no deploy yet. Three and a half days.

**Resume by:** pasting the Quick re-start prompt into a new Claude Code session opened in
`/Users/kurtlozier/Learning/Hamza_Cohort_02_Forward_Deployed_Engineering_Bootcamp/Assignment_1_LUMINA/`.

**Do not:**
- Do not re-open Haiku for planning/research/quick answers, the model split, the same-page
  merge, the docs- or web-mode preflight paths, or stable (gapped) source numbering; OpenRouter
  waits until after eval.
- Do not read `.env` by any means, and do not put `.env` in any Bash line even as a `test -f`
  alongside other commands (the whole line is denied); append-only writes are allowed; process
  env overrides work.
- Do not run multi-collection deletes on Atlas yourself; write the script, hand Kurt the `!` line.
- Do not edit the starter's provided folders, ever — including `benchmark/sla.json` even though
  its comment invites price edits; document the placeholder instead. Do not relax any threshold
  or cap; do not raise `MAX_TOOL_CALLS_DEEP`, `MAX_WALL_CLOCK_SEC_DEEP`, `DEEP_DAILY_CAP`,
  `DEEP_SUB_QUESTIONS_*`, `WEB_PREFLIGHT_MIN_SOURCES`, or the quick caps to pass a gate.
- Do not exceed the $10 Q-4 ceiling without Kurt; a full bench is ≈ $1.05, count them.
- Do not put `plan_research` into `ALL_TOOLS` or `toolsForMode()`; do not emit `plan` after any
  retrieval; do not charge a deep credit when planning fails before retrieval; do not refund one
  after retrieval.
- Do not move cap runs to `runs/failing/`; only error runs go there. Do not let an error run
  sit in `runs/`.
- Do not build the worker on the request thread; do not deploy the agent to Vercel.
- Do not equate bench TTFT with live TTFT; do not pad prompts to reach a cache minimum.
- Do not edit `board/STATE.md` without claiming the lease; never edit a message file or the archive.
- Do not contact Kurt by Telegram unless something is blocking and jointly agreed.
- Do not guess timestamps; use `TZ=America/New_York date`.
- Do not let the session drift toward auto-compaction; recommend `/close resume` at a checkpoint
  and list cheap finish-ups Kurt could take first.
